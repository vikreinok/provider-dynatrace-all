---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: nownotifications.service.dynatrace.m.crossplane.io
spec:
  group: service.dynatrace.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: NowNotification
    listKind: NowNotificationList
    plural: nownotifications
    singular: nownotification
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NowNotification is the Schema for the NowNotifications API. The
          resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NowNotificationSpec defines the desired state of NowNotification
            properties:
              forProvider:
                properties:
                  active:
                    description: |-
                      (Boolean) This setting is enabled (true) or disabled (false)
                      This setting is enabled (`true`) or disabled (`false`)
                    type: boolean
                  events:
                    description: |-
                      (Boolean) Send events into ServiceNow ITOM.
                      Send events into ServiceNow ITOM.
                    type: boolean
                  formatProblemDetailsAsText:
                    description: |-
                      (Boolean) Use text format for problem details instead of HTML.
                      Use text format for problem details instead of HTML.
                    type: boolean
                  incidents:
                    description: |-
                      (Boolean) Send incidents into ServiceNow ITSM.
                      Send incidents into ServiceNow ITSM.
                    type: boolean
                  instance:
                    description: |-
                      (String) The ServiceNow instance identifier. It refers to the first part of your own ServiceNow URL.
                      The ServiceNow instance identifier. It refers to the first part of your own ServiceNow URL.

                      This field is mutually exclusive with the **url** field. You can only use one of them.
                    type: string
                  legacyId:
                    description: |-
                      (String) The ID of these settings when referred to from resources requiring the REST API V1 keys
                      The ID of these settings when referred to from resources requiring the REST API V1 keys
                    type: string
                  message:
                    description: |-
                      (String) The content of the ServiceNow description. Type '{' for placeholder suggestions.. #### Available placeholders
                      {ImpactedEntity}: A short description of the problem and impacted entity (or multiple impacted entities).
                      The content of the ServiceNow description. Type '{' for placeholder suggestions.. #### Available placeholders
                      **{ImpactedEntity}**: A short description of the problem and impacted entity (or multiple impacted entities).

                      **{ImpactedEntityNames}**: The entity impacted by the problem.

                      **{NamesOfImpactedEntities}**: The names of all entities that are impacted by the problem.

                      **{PID}**: Unique system identifier of the reported problem.

                      **{ProblemDetailsHTML}**: All problem event details including root cause as an HTML-formatted string.

                      **{ProblemDetailsText}**: All problem event details including root cause as a text-formatted string.

                      **{ProblemID}**: Display number of the reported problem.

                      **{ProblemImpact}**: Impact level of the problem. Possible values are APPLICATION, SERVICE, or INFRASTRUCTURE.

                      **{ProblemSeverity}**: Severity level of the problem. Possible values are AVAILABILITY, ERROR, PERFORMANCE, RESOURCE_CONTENTION, or CUSTOM_ALERT.

                      **{ProblemTitle}**: Short description of the problem.

                      **{State}**: Problem state. Possible values are OPEN or RESOLVED.

                      **{Tags}**: Comma separated list of tags that are defined for all impacted entities. To refer to the value of a specific tag, specify the tag's key in square brackets: **{Tags[key]}**. If the tag does not have any assigned value, the placeholder will be replaced by an empty string. The placeholder will not be replaced if the tag key does not exist.
                    type: string
                  name:
                    description: |-
                      (String) The name of the notification configuration
                      The name of the notification configuration
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The password to the ServiceNow account.
                      The password to the ServiceNow account.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  profile:
                    description: |-
                      (String) The ID of the associated alerting profile
                      The ID of the associated alerting profile
                    type: string
                  url:
                    description: |-
                      premise ServiceNow installation.
                      The URL of the on-premise ServiceNow installation.

                      This field is mutually exclusive with the **instanceName** field. You can only use one of them.
                    type: string
                  username:
                    description: |-
                      (String) The username of the ServiceNow account.
                      The username of the ServiceNow account.

                      Make sure that your user account has the `web_service_admin` and `x_dynat_ruxit.Integration` roles.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  active:
                    description: |-
                      (Boolean) This setting is enabled (true) or disabled (false)
                      This setting is enabled (`true`) or disabled (`false`)
                    type: boolean
                  events:
                    description: |-
                      (Boolean) Send events into ServiceNow ITOM.
                      Send events into ServiceNow ITOM.
                    type: boolean
                  formatProblemDetailsAsText:
                    description: |-
                      (Boolean) Use text format for problem details instead of HTML.
                      Use text format for problem details instead of HTML.
                    type: boolean
                  incidents:
                    description: |-
                      (Boolean) Send incidents into ServiceNow ITSM.
                      Send incidents into ServiceNow ITSM.
                    type: boolean
                  instance:
                    description: |-
                      (String) The ServiceNow instance identifier. It refers to the first part of your own ServiceNow URL.
                      The ServiceNow instance identifier. It refers to the first part of your own ServiceNow URL.

                      This field is mutually exclusive with the **url** field. You can only use one of them.
                    type: string
                  legacyId:
                    description: |-
                      (String) The ID of these settings when referred to from resources requiring the REST API V1 keys
                      The ID of these settings when referred to from resources requiring the REST API V1 keys
                    type: string
                  message:
                    description: |-
                      (String) The content of the ServiceNow description. Type '{' for placeholder suggestions.. #### Available placeholders
                      {ImpactedEntity}: A short description of the problem and impacted entity (or multiple impacted entities).
                      The content of the ServiceNow description. Type '{' for placeholder suggestions.. #### Available placeholders
                      **{ImpactedEntity}**: A short description of the problem and impacted entity (or multiple impacted entities).

                      **{ImpactedEntityNames}**: The entity impacted by the problem.

                      **{NamesOfImpactedEntities}**: The names of all entities that are impacted by the problem.

                      **{PID}**: Unique system identifier of the reported problem.

                      **{ProblemDetailsHTML}**: All problem event details including root cause as an HTML-formatted string.

                      **{ProblemDetailsText}**: All problem event details including root cause as a text-formatted string.

                      **{ProblemID}**: Display number of the reported problem.

                      **{ProblemImpact}**: Impact level of the problem. Possible values are APPLICATION, SERVICE, or INFRASTRUCTURE.

                      **{ProblemSeverity}**: Severity level of the problem. Possible values are AVAILABILITY, ERROR, PERFORMANCE, RESOURCE_CONTENTION, or CUSTOM_ALERT.

                      **{ProblemTitle}**: Short description of the problem.

                      **{State}**: Problem state. Possible values are OPEN or RESOLVED.

                      **{Tags}**: Comma separated list of tags that are defined for all impacted entities. To refer to the value of a specific tag, specify the tag's key in square brackets: **{Tags[key]}**. If the tag does not have any assigned value, the placeholder will be replaced by an empty string. The placeholder will not be replaced if the tag key does not exist.
                    type: string
                  name:
                    description: |-
                      (String) The name of the notification configuration
                      The name of the notification configuration
                    type: string
                  passwordSecretRef:
                    description: |-
                      (String, Sensitive) The password to the ServiceNow account.
                      The password to the ServiceNow account.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  profile:
                    description: |-
                      (String) The ID of the associated alerting profile
                      The ID of the associated alerting profile
                    type: string
                  url:
                    description: |-
                      premise ServiceNow installation.
                      The URL of the on-premise ServiceNow installation.

                      This field is mutually exclusive with the **instanceName** field. You can only use one of them.
                    type: string
                  username:
                    description: |-
                      (String) The username of the ServiceNow account.
                      The username of the ServiceNow account.

                      Make sure that your user account has the `web_service_admin` and `x_dynat_ruxit.Integration` roles.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.active is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.active)
                || (has(self.initProvider) && has(self.initProvider.active))'
            - message: spec.forProvider.incidents is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.incidents)
                || (has(self.initProvider) && has(self.initProvider.incidents))'
            - message: spec.forProvider.message is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.message)
                || (has(self.initProvider) && has(self.initProvider.message))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.profile is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.profile)
                || (has(self.initProvider) && has(self.initProvider.profile))'
            - message: spec.forProvider.username is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.username)
                || (has(self.initProvider) && has(self.initProvider.username))'
          status:
            description: NowNotificationStatus defines the observed state of NowNotification.
            properties:
              atProvider:
                properties:
                  active:
                    description: |-
                      (Boolean) This setting is enabled (true) or disabled (false)
                      This setting is enabled (`true`) or disabled (`false`)
                    type: boolean
                  events:
                    description: |-
                      (Boolean) Send events into ServiceNow ITOM.
                      Send events into ServiceNow ITOM.
                    type: boolean
                  formatProblemDetailsAsText:
                    description: |-
                      (Boolean) Use text format for problem details instead of HTML.
                      Use text format for problem details instead of HTML.
                    type: boolean
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  incidents:
                    description: |-
                      (Boolean) Send incidents into ServiceNow ITSM.
                      Send incidents into ServiceNow ITSM.
                    type: boolean
                  instance:
                    description: |-
                      (String) The ServiceNow instance identifier. It refers to the first part of your own ServiceNow URL.
                      The ServiceNow instance identifier. It refers to the first part of your own ServiceNow URL.

                      This field is mutually exclusive with the **url** field. You can only use one of them.
                    type: string
                  legacyId:
                    description: |-
                      (String) The ID of these settings when referred to from resources requiring the REST API V1 keys
                      The ID of these settings when referred to from resources requiring the REST API V1 keys
                    type: string
                  message:
                    description: |-
                      (String) The content of the ServiceNow description. Type '{' for placeholder suggestions.. #### Available placeholders
                      {ImpactedEntity}: A short description of the problem and impacted entity (or multiple impacted entities).
                      The content of the ServiceNow description. Type '{' for placeholder suggestions.. #### Available placeholders
                      **{ImpactedEntity}**: A short description of the problem and impacted entity (or multiple impacted entities).

                      **{ImpactedEntityNames}**: The entity impacted by the problem.

                      **{NamesOfImpactedEntities}**: The names of all entities that are impacted by the problem.

                      **{PID}**: Unique system identifier of the reported problem.

                      **{ProblemDetailsHTML}**: All problem event details including root cause as an HTML-formatted string.

                      **{ProblemDetailsText}**: All problem event details including root cause as a text-formatted string.

                      **{ProblemID}**: Display number of the reported problem.

                      **{ProblemImpact}**: Impact level of the problem. Possible values are APPLICATION, SERVICE, or INFRASTRUCTURE.

                      **{ProblemSeverity}**: Severity level of the problem. Possible values are AVAILABILITY, ERROR, PERFORMANCE, RESOURCE_CONTENTION, or CUSTOM_ALERT.

                      **{ProblemTitle}**: Short description of the problem.

                      **{State}**: Problem state. Possible values are OPEN or RESOLVED.

                      **{Tags}**: Comma separated list of tags that are defined for all impacted entities. To refer to the value of a specific tag, specify the tag's key in square brackets: **{Tags[key]}**. If the tag does not have any assigned value, the placeholder will be replaced by an empty string. The placeholder will not be replaced if the tag key does not exist.
                    type: string
                  name:
                    description: |-
                      (String) The name of the notification configuration
                      The name of the notification configuration
                    type: string
                  profile:
                    description: |-
                      (String) The ID of the associated alerting profile
                      The ID of the associated alerting profile
                    type: string
                  url:
                    description: |-
                      premise ServiceNow installation.
                      The URL of the on-premise ServiceNow installation.

                      This field is mutually exclusive with the **instanceName** field. You can only use one of them.
                    type: string
                  username:
                    description: |-
                      (String) The username of the ServiceNow account.
                      The username of the ServiceNow account.

                      Make sure that your user account has the `web_service_admin` and `x_dynat_ruxit.Integration` roles.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
