---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: detectionparameters.failure.dynatrace.m.crossplane.io
spec:
  group: failure.dynatrace.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: DetectionParameters
    listKind: DetectionParametersList
    plural: detectionparameters
    singular: detectionparameters
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DetectionParameters is the Schema for the DetectionParameterss
          API. The resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DetectionParametersSpec defines the desired state of DetectionParameters
            properties:
              forProvider:
                properties:
                  brokenLinks:
                    description: |-
                      side service failures. (see below for nested schema)
                      HTTP 404 response codes are thrown when a web server can't find a certain page. 404s are classified as broken links on the client side and therefore aren't considered to be service failures. By enabling this setting, you can have 404s treated as server-side service failures.
                    items:
                      properties:
                        brokenLinkDomains:
                          description: |-
                            (Set of String) If your application relies on other hosts at other domains, add the associated domain names here. Once configured, Dynatrace will consider 404s thrown by hosts at these domains to be service failures related to your application.
                            If your application relies on other hosts at other domains, add the associated domain names here. Once configured, Dynatrace will consider 404s thrown by hosts at these domains to be service failures related to your application.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        http404NotFoundFailures:
                          description: |-
                            (Boolean) Consider 404 HTTP response codes as failures
                            Consider 404 HTTP response codes as failures
                          type: boolean
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description
                      Description
                    type: string
                  exceptionRules:
                    description: |-
                      (Block List, Min: 1, Max: 1) Customize failure detection for specific exceptions and errors (see below for nested schema)
                      Customize failure detection for specific exceptions and errors
                    items:
                      properties:
                        customErrorRules:
                          description: |-
                            (Block List, Max: 1) Some custom error situations are only detectable via a return value or other means. To support such cases, define a request attribute that captures the required data. Then define a custom error rule that determines if the request has failed based on the value of the request attribute. (see below for nested schema)
                            Some custom error situations are only detectable via a return value or other means. To support such cases, [define a request attribute](https://dt-url.net/ys5k0p4y) that captures the required data. Then define a custom error rule that determines if the request has failed based on the value of the request attribute.
                          items:
                            properties:
                              customErrorRule:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    condition:
                                      description: |-
                                        (Block List, Min: 1, Max: 1) Request attribute condition (see below for nested schema)
                                        Request attribute condition
                                      items:
                                        properties:
                                          caseSensitive:
                                            description: |-
                                              (Boolean) Case sensitive
                                              Case sensitive
                                            type: boolean
                                          compareOperationType:
                                            description: |-
                                              (String) Apply this comparison
                                              Apply this comparison
                                            type: string
                                          doubleValue:
                                            description: |-
                                              (Number) Value
                                              Value
                                            type: number
                                          intValue:
                                            description: |-
                                              (Number) Value
                                              Value
                                            type: number
                                          textValue:
                                            description: |-
                                              (String) Value
                                              Value
                                            type: string
                                        type: object
                                      type: array
                                    requestAttribute:
                                      description: |-
                                        (String) Request attribute
                                        Request attribute
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        customHandledExceptions:
                          description: |-
                            handled exceptions that should be treated as service failures. (see below for nested schema)
                            There may be situations where your application code handles exceptions gracefully in a manner that these failures aren't detected by Dynatrace. Use this setting to define specific gracefully-handled exceptions that should be treated as service failures.
                          items:
                            properties:
                              customHandledException:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    classPattern:
                                      description: |-
                                        (String) The pattern will match if it is contained within the actual class name.
                                        The pattern will match if it is contained within the actual class name.
                                      type: string
                                    messagePattern:
                                      description: |-
                                        (String) Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                        Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        ignoreAllExceptions:
                          description: |-
                            (Boolean) Ignore all exceptions
                            Ignore all exceptions
                          type: boolean
                        ignoreSpanFailureDetection:
                          description: |-
                            (Boolean) Ignore span failure detection
                            Ignore span failure detection
                          type: boolean
                        ignoredExceptions:
                          description: |-
                            party code indicate a specific response, not an error. Use this setting to instruct Dynatrace to treat such exceptions as non-failed requests.. If an exception matching any of the defined patterns occurs in a request, it will not be considered as a failure. Other exceptions occurring at the same request might still mark the request as failed. (see below for nested schema)
                            Some exceptions that are thrown by legacy or 3rd-party code indicate a specific response, not an error. Use this setting to instruct Dynatrace to treat such exceptions as non-failed requests.. If an exception matching any of the defined patterns occurs in a request, it will not be considered as a failure. Other exceptions occurring at the same request might still mark the request as failed.
                          items:
                            properties:
                              customHandledException:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    classPattern:
                                      description: |-
                                        (String) The pattern will match if it is contained within the actual class name.
                                        The pattern will match if it is contained within the actual class name.
                                      type: string
                                    messagePattern:
                                      description: |-
                                        (String) Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                        Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        successForcingExceptions:
                          description: |-
                            (Block List, Max: 1) Define exceptions which indicate that a service call should not be considered as failed. E.g. an exception indicating that the client aborted the operation.. If an exception matching any of the defined patterns occurs on the entry node of the service, it will be considered successful. Compared to ignored exceptions, the request will be considered successful even if other exceptions occur in the same request. (see below for nested schema)
                            Define exceptions which indicate that a service call should not be considered as failed. E.g. an exception indicating that the client aborted the operation.. If an exception matching any of the defined patterns occurs on the entry node of the service, it will be considered successful. Compared to ignored exceptions, the request will be considered successful even if other exceptions occur in the same request.
                          items:
                            properties:
                              customHandledException:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    classPattern:
                                      description: |-
                                        (String) The pattern will match if it is contained within the actual class name.
                                        The pattern will match if it is contained within the actual class name.
                                      type: string
                                    messagePattern:
                                      description: |-
                                        (String) Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                        Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  httpResponseCodes:
                    description: |-
                      (Block List, Min: 1, Max: 1) HTTP response codes (see below for nested schema)
                      HTTP response codes
                    items:
                      properties:
                        clientSideErrors:
                          description: |-
                            (String) HTTP response codes which indicate client side errors
                            HTTP response codes which indicate client side errors
                          type: string
                        failOnMissingResponseCodeClientSide:
                          description: |-
                            (Boolean) Treat missing HTTP response code as client side error
                            Treat missing HTTP response code as client side error
                          type: boolean
                        failOnMissingResponseCodeServerSide:
                          description: |-
                            (Boolean) Treat missing HTTP response code as server side errors
                            Treat missing HTTP response code as server side errors
                          type: boolean
                        serverSideErrors:
                          description: |-
                            (String) HTTP response codes which indicate an error on the server side
                            HTTP response codes which indicate an error on the server side
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name
                      Name
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  brokenLinks:
                    description: |-
                      side service failures. (see below for nested schema)
                      HTTP 404 response codes are thrown when a web server can't find a certain page. 404s are classified as broken links on the client side and therefore aren't considered to be service failures. By enabling this setting, you can have 404s treated as server-side service failures.
                    items:
                      properties:
                        brokenLinkDomains:
                          description: |-
                            (Set of String) If your application relies on other hosts at other domains, add the associated domain names here. Once configured, Dynatrace will consider 404s thrown by hosts at these domains to be service failures related to your application.
                            If your application relies on other hosts at other domains, add the associated domain names here. Once configured, Dynatrace will consider 404s thrown by hosts at these domains to be service failures related to your application.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        http404NotFoundFailures:
                          description: |-
                            (Boolean) Consider 404 HTTP response codes as failures
                            Consider 404 HTTP response codes as failures
                          type: boolean
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description
                      Description
                    type: string
                  exceptionRules:
                    description: |-
                      (Block List, Min: 1, Max: 1) Customize failure detection for specific exceptions and errors (see below for nested schema)
                      Customize failure detection for specific exceptions and errors
                    items:
                      properties:
                        customErrorRules:
                          description: |-
                            (Block List, Max: 1) Some custom error situations are only detectable via a return value or other means. To support such cases, define a request attribute that captures the required data. Then define a custom error rule that determines if the request has failed based on the value of the request attribute. (see below for nested schema)
                            Some custom error situations are only detectable via a return value or other means. To support such cases, [define a request attribute](https://dt-url.net/ys5k0p4y) that captures the required data. Then define a custom error rule that determines if the request has failed based on the value of the request attribute.
                          items:
                            properties:
                              customErrorRule:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    condition:
                                      description: |-
                                        (Block List, Min: 1, Max: 1) Request attribute condition (see below for nested schema)
                                        Request attribute condition
                                      items:
                                        properties:
                                          caseSensitive:
                                            description: |-
                                              (Boolean) Case sensitive
                                              Case sensitive
                                            type: boolean
                                          compareOperationType:
                                            description: |-
                                              (String) Apply this comparison
                                              Apply this comparison
                                            type: string
                                          doubleValue:
                                            description: |-
                                              (Number) Value
                                              Value
                                            type: number
                                          intValue:
                                            description: |-
                                              (Number) Value
                                              Value
                                            type: number
                                          textValue:
                                            description: |-
                                              (String) Value
                                              Value
                                            type: string
                                        type: object
                                      type: array
                                    requestAttribute:
                                      description: |-
                                        (String) Request attribute
                                        Request attribute
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        customHandledExceptions:
                          description: |-
                            handled exceptions that should be treated as service failures. (see below for nested schema)
                            There may be situations where your application code handles exceptions gracefully in a manner that these failures aren't detected by Dynatrace. Use this setting to define specific gracefully-handled exceptions that should be treated as service failures.
                          items:
                            properties:
                              customHandledException:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    classPattern:
                                      description: |-
                                        (String) The pattern will match if it is contained within the actual class name.
                                        The pattern will match if it is contained within the actual class name.
                                      type: string
                                    messagePattern:
                                      description: |-
                                        (String) Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                        Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        ignoreAllExceptions:
                          description: |-
                            (Boolean) Ignore all exceptions
                            Ignore all exceptions
                          type: boolean
                        ignoreSpanFailureDetection:
                          description: |-
                            (Boolean) Ignore span failure detection
                            Ignore span failure detection
                          type: boolean
                        ignoredExceptions:
                          description: |-
                            party code indicate a specific response, not an error. Use this setting to instruct Dynatrace to treat such exceptions as non-failed requests.. If an exception matching any of the defined patterns occurs in a request, it will not be considered as a failure. Other exceptions occurring at the same request might still mark the request as failed. (see below for nested schema)
                            Some exceptions that are thrown by legacy or 3rd-party code indicate a specific response, not an error. Use this setting to instruct Dynatrace to treat such exceptions as non-failed requests.. If an exception matching any of the defined patterns occurs in a request, it will not be considered as a failure. Other exceptions occurring at the same request might still mark the request as failed.
                          items:
                            properties:
                              customHandledException:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    classPattern:
                                      description: |-
                                        (String) The pattern will match if it is contained within the actual class name.
                                        The pattern will match if it is contained within the actual class name.
                                      type: string
                                    messagePattern:
                                      description: |-
                                        (String) Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                        Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        successForcingExceptions:
                          description: |-
                            (Block List, Max: 1) Define exceptions which indicate that a service call should not be considered as failed. E.g. an exception indicating that the client aborted the operation.. If an exception matching any of the defined patterns occurs on the entry node of the service, it will be considered successful. Compared to ignored exceptions, the request will be considered successful even if other exceptions occur in the same request. (see below for nested schema)
                            Define exceptions which indicate that a service call should not be considered as failed. E.g. an exception indicating that the client aborted the operation.. If an exception matching any of the defined patterns occurs on the entry node of the service, it will be considered successful. Compared to ignored exceptions, the request will be considered successful even if other exceptions occur in the same request.
                          items:
                            properties:
                              customHandledException:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    classPattern:
                                      description: |-
                                        (String) The pattern will match if it is contained within the actual class name.
                                        The pattern will match if it is contained within the actual class name.
                                      type: string
                                    messagePattern:
                                      description: |-
                                        (String) Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                        Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  httpResponseCodes:
                    description: |-
                      (Block List, Min: 1, Max: 1) HTTP response codes (see below for nested schema)
                      HTTP response codes
                    items:
                      properties:
                        clientSideErrors:
                          description: |-
                            (String) HTTP response codes which indicate client side errors
                            HTTP response codes which indicate client side errors
                          type: string
                        failOnMissingResponseCodeClientSide:
                          description: |-
                            (Boolean) Treat missing HTTP response code as client side error
                            Treat missing HTTP response code as client side error
                          type: boolean
                        failOnMissingResponseCodeServerSide:
                          description: |-
                            (Boolean) Treat missing HTTP response code as server side errors
                            Treat missing HTTP response code as server side errors
                          type: boolean
                        serverSideErrors:
                          description: |-
                            (String) HTTP response codes which indicate an error on the server side
                            HTTP response codes which indicate an error on the server side
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      (String) Name
                      Name
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.brokenLinks is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.brokenLinks)
                || (has(self.initProvider) && has(self.initProvider.brokenLinks))'
            - message: spec.forProvider.exceptionRules is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.exceptionRules)
                || (has(self.initProvider) && has(self.initProvider.exceptionRules))'
            - message: spec.forProvider.httpResponseCodes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.httpResponseCodes)
                || (has(self.initProvider) && has(self.initProvider.httpResponseCodes))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: DetectionParametersStatus defines the observed state of DetectionParameters.
            properties:
              atProvider:
                properties:
                  brokenLinks:
                    description: |-
                      side service failures. (see below for nested schema)
                      HTTP 404 response codes are thrown when a web server can't find a certain page. 404s are classified as broken links on the client side and therefore aren't considered to be service failures. By enabling this setting, you can have 404s treated as server-side service failures.
                    items:
                      properties:
                        brokenLinkDomains:
                          description: |-
                            (Set of String) If your application relies on other hosts at other domains, add the associated domain names here. Once configured, Dynatrace will consider 404s thrown by hosts at these domains to be service failures related to your application.
                            If your application relies on other hosts at other domains, add the associated domain names here. Once configured, Dynatrace will consider 404s thrown by hosts at these domains to be service failures related to your application.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        http404NotFoundFailures:
                          description: |-
                            (Boolean) Consider 404 HTTP response codes as failures
                            Consider 404 HTTP response codes as failures
                          type: boolean
                      type: object
                    type: array
                  description:
                    description: |-
                      (String) Description
                      Description
                    type: string
                  exceptionRules:
                    description: |-
                      (Block List, Min: 1, Max: 1) Customize failure detection for specific exceptions and errors (see below for nested schema)
                      Customize failure detection for specific exceptions and errors
                    items:
                      properties:
                        customErrorRules:
                          description: |-
                            (Block List, Max: 1) Some custom error situations are only detectable via a return value or other means. To support such cases, define a request attribute that captures the required data. Then define a custom error rule that determines if the request has failed based on the value of the request attribute. (see below for nested schema)
                            Some custom error situations are only detectable via a return value or other means. To support such cases, [define a request attribute](https://dt-url.net/ys5k0p4y) that captures the required data. Then define a custom error rule that determines if the request has failed based on the value of the request attribute.
                          items:
                            properties:
                              customErrorRule:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    condition:
                                      description: |-
                                        (Block List, Min: 1, Max: 1) Request attribute condition (see below for nested schema)
                                        Request attribute condition
                                      items:
                                        properties:
                                          caseSensitive:
                                            description: |-
                                              (Boolean) Case sensitive
                                              Case sensitive
                                            type: boolean
                                          compareOperationType:
                                            description: |-
                                              (String) Apply this comparison
                                              Apply this comparison
                                            type: string
                                          doubleValue:
                                            description: |-
                                              (Number) Value
                                              Value
                                            type: number
                                          intValue:
                                            description: |-
                                              (Number) Value
                                              Value
                                            type: number
                                          textValue:
                                            description: |-
                                              (String) Value
                                              Value
                                            type: string
                                        type: object
                                      type: array
                                    requestAttribute:
                                      description: |-
                                        (String) Request attribute
                                        Request attribute
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        customHandledExceptions:
                          description: |-
                            handled exceptions that should be treated as service failures. (see below for nested schema)
                            There may be situations where your application code handles exceptions gracefully in a manner that these failures aren't detected by Dynatrace. Use this setting to define specific gracefully-handled exceptions that should be treated as service failures.
                          items:
                            properties:
                              customHandledException:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    classPattern:
                                      description: |-
                                        (String) The pattern will match if it is contained within the actual class name.
                                        The pattern will match if it is contained within the actual class name.
                                      type: string
                                    messagePattern:
                                      description: |-
                                        (String) Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                        Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        ignoreAllExceptions:
                          description: |-
                            (Boolean) Ignore all exceptions
                            Ignore all exceptions
                          type: boolean
                        ignoreSpanFailureDetection:
                          description: |-
                            (Boolean) Ignore span failure detection
                            Ignore span failure detection
                          type: boolean
                        ignoredExceptions:
                          description: |-
                            party code indicate a specific response, not an error. Use this setting to instruct Dynatrace to treat such exceptions as non-failed requests.. If an exception matching any of the defined patterns occurs in a request, it will not be considered as a failure. Other exceptions occurring at the same request might still mark the request as failed. (see below for nested schema)
                            Some exceptions that are thrown by legacy or 3rd-party code indicate a specific response, not an error. Use this setting to instruct Dynatrace to treat such exceptions as non-failed requests.. If an exception matching any of the defined patterns occurs in a request, it will not be considered as a failure. Other exceptions occurring at the same request might still mark the request as failed.
                          items:
                            properties:
                              customHandledException:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    classPattern:
                                      description: |-
                                        (String) The pattern will match if it is contained within the actual class name.
                                        The pattern will match if it is contained within the actual class name.
                                      type: string
                                    messagePattern:
                                      description: |-
                                        (String) Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                        Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        successForcingExceptions:
                          description: |-
                            (Block List, Max: 1) Define exceptions which indicate that a service call should not be considered as failed. E.g. an exception indicating that the client aborted the operation.. If an exception matching any of the defined patterns occurs on the entry node of the service, it will be considered successful. Compared to ignored exceptions, the request will be considered successful even if other exceptions occur in the same request. (see below for nested schema)
                            Define exceptions which indicate that a service call should not be considered as failed. E.g. an exception indicating that the client aborted the operation.. If an exception matching any of the defined patterns occurs on the entry node of the service, it will be considered successful. Compared to ignored exceptions, the request will be considered successful even if other exceptions occur in the same request.
                          items:
                            properties:
                              customHandledException:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    classPattern:
                                      description: |-
                                        (String) The pattern will match if it is contained within the actual class name.
                                        The pattern will match if it is contained within the actual class name.
                                      type: string
                                    messagePattern:
                                      description: |-
                                        (String) Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                        Optionally, define an exception message pattern. The pattern will match if the actual exception message contains the pattern.
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  httpResponseCodes:
                    description: |-
                      (Block List, Min: 1, Max: 1) HTTP response codes (see below for nested schema)
                      HTTP response codes
                    items:
                      properties:
                        clientSideErrors:
                          description: |-
                            (String) HTTP response codes which indicate client side errors
                            HTTP response codes which indicate client side errors
                          type: string
                        failOnMissingResponseCodeClientSide:
                          description: |-
                            (Boolean) Treat missing HTTP response code as client side error
                            Treat missing HTTP response code as client side error
                          type: boolean
                        failOnMissingResponseCodeServerSide:
                          description: |-
                            (Boolean) Treat missing HTTP response code as server side errors
                            Treat missing HTTP response code as server side errors
                          type: boolean
                        serverSideErrors:
                          description: |-
                            (String) HTTP response codes which indicate an error on the server side
                            HTTP response codes which indicate an error on the server side
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  name:
                    description: |-
                      (String) Name
                      Name
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
