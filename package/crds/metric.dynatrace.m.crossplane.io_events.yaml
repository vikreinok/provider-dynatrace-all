---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: events.metric.dynatrace.m.crossplane.io
spec:
  group: metric.dynatrace.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: Events
    listKind: EventsList
    plural: events
    singular: events
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Events is the Schema for the Eventss API. The resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventsSpec defines the desired state of Events
            properties:
              forProvider:
                properties:
                  enabled:
                    description: |-
                      (Boolean) This setting is enabled (true) or disabled (false)
                      This setting is enabled (`true`) or disabled (`false`)
                    type: boolean
                  eventEntityDimensionKey:
                    description: |-
                      (String) Controls the preferred entity type used for triggered events.
                      Controls the preferred entity type used for triggered events.
                    type: string
                  eventTemplate:
                    description: |-
                      (Block List, Min: 1, Max: 1) Event template (see below for nested schema)
                      Event template
                    items:
                      properties:
                        davisMerge:
                          description: |-
                            (Boolean) Davis® AI will try to merge this event into existing problems, otherwise a new problem will always be created.
                            Davis® AI will try to merge this event into existing problems, otherwise a new problem will always be created.
                          type: boolean
                        description:
                          description: |-
                            (String) The description of the event to trigger.
                            The description of the event to trigger.
                          type: string
                        eventType:
                          description: |-
                            (String) Possible Values: AVAILABILITY, CUSTOM_ALERT, CUSTOM_ANNOTATION, CUSTOM_CONFIGURATION, CUSTOM_DEPLOYMENT, ERROR, INFO, MARKED_FOR_TERMINATION, RESOURCE, SLOWDOWN, WARNING
                            Possible Values: `AVAILABILITY`, `CUSTOM_ALERT`, `CUSTOM_ANNOTATION`, `CUSTOM_CONFIGURATION`, `CUSTOM_DEPLOYMENT`, `ERROR`, `INFO`, `MARKED_FOR_TERMINATION`, `RESOURCE`, `SLOWDOWN`, `WARNING`
                          type: string
                        metadata:
                          description: |-
                            value properties to be attached to the triggered event. (see below for nested schema)
                            Set of additional key-value properties to be attached to the triggered event.
                          items:
                            properties:
                              metadataKey:
                                description: |-
                                  (String) Type 'dt.' for key hints.
                                  Type 'dt.' for key hints.
                                type: string
                              metadataValue:
                                description: |-
                                  (String) no documentation available
                                  no documentation available
                                type: string
                            type: object
                          type: array
                        title:
                          description: |-
                            (String) The title of the event to trigger.
                            The title of the event to trigger.
                          type: string
                      type: object
                    type: array
                  legacyId:
                    description: |-
                      (String) Config id
                      Config id
                    type: string
                  modelProperties:
                    description: |-
                      (Block List, Min: 1, Max: 1) Monitoring strategy (see below for nested schema)
                      Monitoring strategy
                    items:
                      properties:
                        alertCondition:
                          description: |-
                            (String) Possible Values: ABOVE, BELOW, OUTSIDE
                            Possible Values: `ABOVE`, `BELOW`, `OUTSIDE`
                          type: string
                        alertOnNoData:
                          description: |-
                            (Boolean) The ability to set an alert on missing data in a metric. When enabled, missing data samples will be treated as violating samples defined in the advanced model properties. When disabled, missing data is not treated as a violation but will still contribute to dealerting. We recommend disabling alerting on missing data for sparse timeseries to avoid false alerts. To learn more, visit anomaly detection configuration.
                            The ability to set an alert on missing data in a metric. When enabled, missing data samples will be treated as violating samples defined in the advanced model properties. When disabled, missing data is not treated as a violation but will still contribute to dealerting. We recommend disabling alerting on missing data for sparse timeseries to avoid false alerts. To learn more, visit [anomaly detection configuration](https://dt-url.net/lz02mwi).
                          type: boolean
                        dealertingSamples:
                          description: |-
                            minute samples within the evaluation window that must go back to normal to close the event.
                            The number of one-minute samples within the evaluation window that must go back to normal to close the event.
                          type: number
                        samples:
                          description: |-
                            minute samples that form the sliding evaluation window.
                            The number of one-minute samples that form the sliding evaluation window.
                          type: number
                        signalFluctuation:
                          description: |-
                            (Number) Controls how many times the signal fluctuation is added to the baseline to produce the actual threshold for alerting
                            Controls how many times the signal fluctuation is added to the baseline to produce the actual threshold for alerting
                          type: number
                        threshold:
                          description: |-
                            (Number) Raise an event if this value is violated
                            Raise an event if this value is violated
                          type: number
                        tolerance:
                          description: |-
                            (Number) Controls the width of the confidence band and larger values lead to a less sensitive model
                            Controls the width of the confidence band and larger values lead to a less sensitive model
                          type: number
                        type:
                          description: |-
                            (String) Possible Values: AUTO_ADAPTIVE_THRESHOLD, SEASONAL_BASELINE, STATIC_THRESHOLD
                            Possible Values: `AUTO_ADAPTIVE_THRESHOLD`, `SEASONAL_BASELINE`, `STATIC_THRESHOLD`
                          type: string
                        violatingSamples:
                          description: |-
                            minute samples within the evaluation window that must violate to trigger an event.
                            The number of one-minute samples within the evaluation window that must violate to trigger an event.
                          type: number
                      type: object
                    type: array
                  queryDefinition:
                    description: |-
                      (Block List, Min: 1, Max: 1) Query definition (see below for nested schema)
                      Query definition
                    items:
                      properties:
                        aggregation:
                          description: |-
                            (String) Possible Values: AVG, COUNT, MAX, MEDIAN, MIN, PERCENTILE90, SUM, VALUE
                            Possible Values: `AVG`, `COUNT`, `MAX`, `MEDIAN`, `MIN`, `PERCENTILE90`, `SUM`, `VALUE`
                          type: string
                        dimensionFilter:
                          description: |-
                            (Block List, Max: 1) Dimension filter (see below for nested schema)
                            Dimension filter
                          items:
                            properties:
                              filter:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    dimensionKey:
                                      description: |-
                                        (String) Dimension key
                                        Dimension key
                                      type: string
                                    dimensionValue:
                                      description: |-
                                        (String) Dimension value
                                        Dimension value
                                      type: string
                                    operator:
                                      description: |-
                                        (String) Possible Values: CONTAINS_CASE_SENSITIVE, DOES_NOT_CONTAIN_CASE_SENSITIVE, DOES_NOT_EQUAL, DOES_NOT_START_WITH, EQUALS, STARTS_WITH
                                        Possible Values: `CONTAINS_CASE_SENSITIVE`, `DOES_NOT_CONTAIN_CASE_SENSITIVE`, `DOES_NOT_EQUAL`, `DOES_NOT_START_WITH`, `EQUALS`, `STARTS_WITH`
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        entityFilter:
                          description: |-
                            based filters to define the scope this event monitors. (see below for nested schema)
                            Use rule-based filters to define the scope this event monitors.
                          items:
                            properties:
                              conditions:
                                description: |-
                                  (Block List, Max: 1) no documentation available (see below for nested schema)
                                  no documentation available
                                items:
                                  properties:
                                    condition:
                                      description: '(Block Set, Min: 1) (see below
                                        for nested schema)'
                                      items:
                                        properties:
                                          operator:
                                            description: |-
                                              (String) Possible Values: CONTAINS_CASE_SENSITIVE, DOES_NOT_CONTAIN_CASE_SENSITIVE, DOES_NOT_EQUAL, DOES_NOT_START_WITH, EQUALS, STARTS_WITH
                                              Possible Values: `CONTAINS_CASE_INSENSITIVE`, `CONTAINS_CASE_SENSITIVE`, `DOES_NOT_CONTAIN_CASE_INSENSITIVE`, `DOES_NOT_CONTAIN_CASE_SENSITIVE`, `DOES_NOT_EQUAL`, `DOES_NOT_START_WITH`, `EQUALS`, `STARTS_WITH`
                                            type: string
                                          type:
                                            description: |-
                                              (String) Possible Values: AUTO_ADAPTIVE_THRESHOLD, SEASONAL_BASELINE, STATIC_THRESHOLD
                                              Possible Values: `CUSTOM_DEVICE_GROUP_NAME`, `ENTITY_ID`, `HOST_GROUP_NAME`, `HOST_NAME`, `MANAGEMENT_ZONE`, `NAME`, `PROCESS_GROUP_ID`, `PROCESS_GROUP_NAME`, `TAG`
                                            type: string
                                          value:
                                            description: |-
                                              (String) no documentation available
                                              no documentation available
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              dimensionKey:
                                description: |-
                                  (String) Dimension key
                                  Dimension key of entity type to filter
                                type: string
                            type: object
                          type: array
                        managementZone:
                          description: |-
                            (String) The legacy_id of a Management Zone (as provided by the resource dynatrace_management_zone_v2 or the data source dynatrace_management_zone)
                            The `legacy_id` of a Management Zone (as provided by the resource `dynatrace_management_zone_v2` or the data source `dynatrace_management_zone`)
                          type: string
                        metricKey:
                          description: |-
                            (String) Metric key
                            Metric key
                          type: string
                        metricSelector:
                          description: |-
                            (String) To learn more, visit Metric Selector
                            To learn more, visit [Metric Selector](https://dt-url.net/metselad)
                          type: string
                        queryOffset:
                          description: |-
                            (Number) Minute offset of sliding evaluation window for metrics with latency
                            Minute offset of sliding evaluation window for metrics with latency
                          type: number
                        type:
                          description: |-
                            (String) Possible Values: AUTO_ADAPTIVE_THRESHOLD, SEASONAL_BASELINE, STATIC_THRESHOLD
                            Possible Values: `METRIC_KEY`, `METRIC_SELECTOR`
                          type: string
                      type: object
                    type: array
                  summary:
                    description: |-
                      (String) The textual summary of the metric event entry
                      The textual summary of the metric event entry
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  enabled:
                    description: |-
                      (Boolean) This setting is enabled (true) or disabled (false)
                      This setting is enabled (`true`) or disabled (`false`)
                    type: boolean
                  eventEntityDimensionKey:
                    description: |-
                      (String) Controls the preferred entity type used for triggered events.
                      Controls the preferred entity type used for triggered events.
                    type: string
                  eventTemplate:
                    description: |-
                      (Block List, Min: 1, Max: 1) Event template (see below for nested schema)
                      Event template
                    items:
                      properties:
                        davisMerge:
                          description: |-
                            (Boolean) Davis® AI will try to merge this event into existing problems, otherwise a new problem will always be created.
                            Davis® AI will try to merge this event into existing problems, otherwise a new problem will always be created.
                          type: boolean
                        description:
                          description: |-
                            (String) The description of the event to trigger.
                            The description of the event to trigger.
                          type: string
                        eventType:
                          description: |-
                            (String) Possible Values: AVAILABILITY, CUSTOM_ALERT, CUSTOM_ANNOTATION, CUSTOM_CONFIGURATION, CUSTOM_DEPLOYMENT, ERROR, INFO, MARKED_FOR_TERMINATION, RESOURCE, SLOWDOWN, WARNING
                            Possible Values: `AVAILABILITY`, `CUSTOM_ALERT`, `CUSTOM_ANNOTATION`, `CUSTOM_CONFIGURATION`, `CUSTOM_DEPLOYMENT`, `ERROR`, `INFO`, `MARKED_FOR_TERMINATION`, `RESOURCE`, `SLOWDOWN`, `WARNING`
                          type: string
                        metadata:
                          description: |-
                            value properties to be attached to the triggered event. (see below for nested schema)
                            Set of additional key-value properties to be attached to the triggered event.
                          items:
                            properties:
                              metadataKey:
                                description: |-
                                  (String) Type 'dt.' for key hints.
                                  Type 'dt.' for key hints.
                                type: string
                              metadataValue:
                                description: |-
                                  (String) no documentation available
                                  no documentation available
                                type: string
                            type: object
                          type: array
                        title:
                          description: |-
                            (String) The title of the event to trigger.
                            The title of the event to trigger.
                          type: string
                      type: object
                    type: array
                  legacyId:
                    description: |-
                      (String) Config id
                      Config id
                    type: string
                  modelProperties:
                    description: |-
                      (Block List, Min: 1, Max: 1) Monitoring strategy (see below for nested schema)
                      Monitoring strategy
                    items:
                      properties:
                        alertCondition:
                          description: |-
                            (String) Possible Values: ABOVE, BELOW, OUTSIDE
                            Possible Values: `ABOVE`, `BELOW`, `OUTSIDE`
                          type: string
                        alertOnNoData:
                          description: |-
                            (Boolean) The ability to set an alert on missing data in a metric. When enabled, missing data samples will be treated as violating samples defined in the advanced model properties. When disabled, missing data is not treated as a violation but will still contribute to dealerting. We recommend disabling alerting on missing data for sparse timeseries to avoid false alerts. To learn more, visit anomaly detection configuration.
                            The ability to set an alert on missing data in a metric. When enabled, missing data samples will be treated as violating samples defined in the advanced model properties. When disabled, missing data is not treated as a violation but will still contribute to dealerting. We recommend disabling alerting on missing data for sparse timeseries to avoid false alerts. To learn more, visit [anomaly detection configuration](https://dt-url.net/lz02mwi).
                          type: boolean
                        dealertingSamples:
                          description: |-
                            minute samples within the evaluation window that must go back to normal to close the event.
                            The number of one-minute samples within the evaluation window that must go back to normal to close the event.
                          type: number
                        samples:
                          description: |-
                            minute samples that form the sliding evaluation window.
                            The number of one-minute samples that form the sliding evaluation window.
                          type: number
                        signalFluctuation:
                          description: |-
                            (Number) Controls how many times the signal fluctuation is added to the baseline to produce the actual threshold for alerting
                            Controls how many times the signal fluctuation is added to the baseline to produce the actual threshold for alerting
                          type: number
                        threshold:
                          description: |-
                            (Number) Raise an event if this value is violated
                            Raise an event if this value is violated
                          type: number
                        tolerance:
                          description: |-
                            (Number) Controls the width of the confidence band and larger values lead to a less sensitive model
                            Controls the width of the confidence band and larger values lead to a less sensitive model
                          type: number
                        type:
                          description: |-
                            (String) Possible Values: AUTO_ADAPTIVE_THRESHOLD, SEASONAL_BASELINE, STATIC_THRESHOLD
                            Possible Values: `AUTO_ADAPTIVE_THRESHOLD`, `SEASONAL_BASELINE`, `STATIC_THRESHOLD`
                          type: string
                        violatingSamples:
                          description: |-
                            minute samples within the evaluation window that must violate to trigger an event.
                            The number of one-minute samples within the evaluation window that must violate to trigger an event.
                          type: number
                      type: object
                    type: array
                  queryDefinition:
                    description: |-
                      (Block List, Min: 1, Max: 1) Query definition (see below for nested schema)
                      Query definition
                    items:
                      properties:
                        aggregation:
                          description: |-
                            (String) Possible Values: AVG, COUNT, MAX, MEDIAN, MIN, PERCENTILE90, SUM, VALUE
                            Possible Values: `AVG`, `COUNT`, `MAX`, `MEDIAN`, `MIN`, `PERCENTILE90`, `SUM`, `VALUE`
                          type: string
                        dimensionFilter:
                          description: |-
                            (Block List, Max: 1) Dimension filter (see below for nested schema)
                            Dimension filter
                          items:
                            properties:
                              filter:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    dimensionKey:
                                      description: |-
                                        (String) Dimension key
                                        Dimension key
                                      type: string
                                    dimensionValue:
                                      description: |-
                                        (String) Dimension value
                                        Dimension value
                                      type: string
                                    operator:
                                      description: |-
                                        (String) Possible Values: CONTAINS_CASE_SENSITIVE, DOES_NOT_CONTAIN_CASE_SENSITIVE, DOES_NOT_EQUAL, DOES_NOT_START_WITH, EQUALS, STARTS_WITH
                                        Possible Values: `CONTAINS_CASE_SENSITIVE`, `DOES_NOT_CONTAIN_CASE_SENSITIVE`, `DOES_NOT_EQUAL`, `DOES_NOT_START_WITH`, `EQUALS`, `STARTS_WITH`
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        entityFilter:
                          description: |-
                            based filters to define the scope this event monitors. (see below for nested schema)
                            Use rule-based filters to define the scope this event monitors.
                          items:
                            properties:
                              conditions:
                                description: |-
                                  (Block List, Max: 1) no documentation available (see below for nested schema)
                                  no documentation available
                                items:
                                  properties:
                                    condition:
                                      description: '(Block Set, Min: 1) (see below
                                        for nested schema)'
                                      items:
                                        properties:
                                          operator:
                                            description: |-
                                              (String) Possible Values: CONTAINS_CASE_SENSITIVE, DOES_NOT_CONTAIN_CASE_SENSITIVE, DOES_NOT_EQUAL, DOES_NOT_START_WITH, EQUALS, STARTS_WITH
                                              Possible Values: `CONTAINS_CASE_INSENSITIVE`, `CONTAINS_CASE_SENSITIVE`, `DOES_NOT_CONTAIN_CASE_INSENSITIVE`, `DOES_NOT_CONTAIN_CASE_SENSITIVE`, `DOES_NOT_EQUAL`, `DOES_NOT_START_WITH`, `EQUALS`, `STARTS_WITH`
                                            type: string
                                          type:
                                            description: |-
                                              (String) Possible Values: AUTO_ADAPTIVE_THRESHOLD, SEASONAL_BASELINE, STATIC_THRESHOLD
                                              Possible Values: `CUSTOM_DEVICE_GROUP_NAME`, `ENTITY_ID`, `HOST_GROUP_NAME`, `HOST_NAME`, `MANAGEMENT_ZONE`, `NAME`, `PROCESS_GROUP_ID`, `PROCESS_GROUP_NAME`, `TAG`
                                            type: string
                                          value:
                                            description: |-
                                              (String) no documentation available
                                              no documentation available
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              dimensionKey:
                                description: |-
                                  (String) Dimension key
                                  Dimension key of entity type to filter
                                type: string
                            type: object
                          type: array
                        managementZone:
                          description: |-
                            (String) The legacy_id of a Management Zone (as provided by the resource dynatrace_management_zone_v2 or the data source dynatrace_management_zone)
                            The `legacy_id` of a Management Zone (as provided by the resource `dynatrace_management_zone_v2` or the data source `dynatrace_management_zone`)
                          type: string
                        metricKey:
                          description: |-
                            (String) Metric key
                            Metric key
                          type: string
                        metricSelector:
                          description: |-
                            (String) To learn more, visit Metric Selector
                            To learn more, visit [Metric Selector](https://dt-url.net/metselad)
                          type: string
                        queryOffset:
                          description: |-
                            (Number) Minute offset of sliding evaluation window for metrics with latency
                            Minute offset of sliding evaluation window for metrics with latency
                          type: number
                        type:
                          description: |-
                            (String) Possible Values: AUTO_ADAPTIVE_THRESHOLD, SEASONAL_BASELINE, STATIC_THRESHOLD
                            Possible Values: `METRIC_KEY`, `METRIC_SELECTOR`
                          type: string
                      type: object
                    type: array
                  summary:
                    description: |-
                      (String) The textual summary of the metric event entry
                      The textual summary of the metric event entry
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.enabled is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enabled)
                || (has(self.initProvider) && has(self.initProvider.enabled))'
            - message: spec.forProvider.eventTemplate is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.eventTemplate)
                || (has(self.initProvider) && has(self.initProvider.eventTemplate))'
            - message: spec.forProvider.modelProperties is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.modelProperties)
                || (has(self.initProvider) && has(self.initProvider.modelProperties))'
            - message: spec.forProvider.queryDefinition is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.queryDefinition)
                || (has(self.initProvider) && has(self.initProvider.queryDefinition))'
            - message: spec.forProvider.summary is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.summary)
                || (has(self.initProvider) && has(self.initProvider.summary))'
          status:
            description: EventsStatus defines the observed state of Events.
            properties:
              atProvider:
                properties:
                  enabled:
                    description: |-
                      (Boolean) This setting is enabled (true) or disabled (false)
                      This setting is enabled (`true`) or disabled (`false`)
                    type: boolean
                  eventEntityDimensionKey:
                    description: |-
                      (String) Controls the preferred entity type used for triggered events.
                      Controls the preferred entity type used for triggered events.
                    type: string
                  eventTemplate:
                    description: |-
                      (Block List, Min: 1, Max: 1) Event template (see below for nested schema)
                      Event template
                    items:
                      properties:
                        davisMerge:
                          description: |-
                            (Boolean) Davis® AI will try to merge this event into existing problems, otherwise a new problem will always be created.
                            Davis® AI will try to merge this event into existing problems, otherwise a new problem will always be created.
                          type: boolean
                        description:
                          description: |-
                            (String) The description of the event to trigger.
                            The description of the event to trigger.
                          type: string
                        eventType:
                          description: |-
                            (String) Possible Values: AVAILABILITY, CUSTOM_ALERT, CUSTOM_ANNOTATION, CUSTOM_CONFIGURATION, CUSTOM_DEPLOYMENT, ERROR, INFO, MARKED_FOR_TERMINATION, RESOURCE, SLOWDOWN, WARNING
                            Possible Values: `AVAILABILITY`, `CUSTOM_ALERT`, `CUSTOM_ANNOTATION`, `CUSTOM_CONFIGURATION`, `CUSTOM_DEPLOYMENT`, `ERROR`, `INFO`, `MARKED_FOR_TERMINATION`, `RESOURCE`, `SLOWDOWN`, `WARNING`
                          type: string
                        metadata:
                          description: |-
                            value properties to be attached to the triggered event. (see below for nested schema)
                            Set of additional key-value properties to be attached to the triggered event.
                          items:
                            properties:
                              metadataKey:
                                description: |-
                                  (String) Type 'dt.' for key hints.
                                  Type 'dt.' for key hints.
                                type: string
                              metadataValue:
                                description: |-
                                  (String) no documentation available
                                  no documentation available
                                type: string
                            type: object
                          type: array
                        title:
                          description: |-
                            (String) The title of the event to trigger.
                            The title of the event to trigger.
                          type: string
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  legacyId:
                    description: |-
                      (String) Config id
                      Config id
                    type: string
                  modelProperties:
                    description: |-
                      (Block List, Min: 1, Max: 1) Monitoring strategy (see below for nested schema)
                      Monitoring strategy
                    items:
                      properties:
                        alertCondition:
                          description: |-
                            (String) Possible Values: ABOVE, BELOW, OUTSIDE
                            Possible Values: `ABOVE`, `BELOW`, `OUTSIDE`
                          type: string
                        alertOnNoData:
                          description: |-
                            (Boolean) The ability to set an alert on missing data in a metric. When enabled, missing data samples will be treated as violating samples defined in the advanced model properties. When disabled, missing data is not treated as a violation but will still contribute to dealerting. We recommend disabling alerting on missing data for sparse timeseries to avoid false alerts. To learn more, visit anomaly detection configuration.
                            The ability to set an alert on missing data in a metric. When enabled, missing data samples will be treated as violating samples defined in the advanced model properties. When disabled, missing data is not treated as a violation but will still contribute to dealerting. We recommend disabling alerting on missing data for sparse timeseries to avoid false alerts. To learn more, visit [anomaly detection configuration](https://dt-url.net/lz02mwi).
                          type: boolean
                        dealertingSamples:
                          description: |-
                            minute samples within the evaluation window that must go back to normal to close the event.
                            The number of one-minute samples within the evaluation window that must go back to normal to close the event.
                          type: number
                        samples:
                          description: |-
                            minute samples that form the sliding evaluation window.
                            The number of one-minute samples that form the sliding evaluation window.
                          type: number
                        signalFluctuation:
                          description: |-
                            (Number) Controls how many times the signal fluctuation is added to the baseline to produce the actual threshold for alerting
                            Controls how many times the signal fluctuation is added to the baseline to produce the actual threshold for alerting
                          type: number
                        threshold:
                          description: |-
                            (Number) Raise an event if this value is violated
                            Raise an event if this value is violated
                          type: number
                        tolerance:
                          description: |-
                            (Number) Controls the width of the confidence band and larger values lead to a less sensitive model
                            Controls the width of the confidence band and larger values lead to a less sensitive model
                          type: number
                        type:
                          description: |-
                            (String) Possible Values: AUTO_ADAPTIVE_THRESHOLD, SEASONAL_BASELINE, STATIC_THRESHOLD
                            Possible Values: `AUTO_ADAPTIVE_THRESHOLD`, `SEASONAL_BASELINE`, `STATIC_THRESHOLD`
                          type: string
                        violatingSamples:
                          description: |-
                            minute samples within the evaluation window that must violate to trigger an event.
                            The number of one-minute samples within the evaluation window that must violate to trigger an event.
                          type: number
                      type: object
                    type: array
                  queryDefinition:
                    description: |-
                      (Block List, Min: 1, Max: 1) Query definition (see below for nested schema)
                      Query definition
                    items:
                      properties:
                        aggregation:
                          description: |-
                            (String) Possible Values: AVG, COUNT, MAX, MEDIAN, MIN, PERCENTILE90, SUM, VALUE
                            Possible Values: `AVG`, `COUNT`, `MAX`, `MEDIAN`, `MIN`, `PERCENTILE90`, `SUM`, `VALUE`
                          type: string
                        dimensionFilter:
                          description: |-
                            (Block List, Max: 1) Dimension filter (see below for nested schema)
                            Dimension filter
                          items:
                            properties:
                              filter:
                                description: '(Block Set, Min: 1) (see below for nested
                                  schema)'
                                items:
                                  properties:
                                    dimensionKey:
                                      description: |-
                                        (String) Dimension key
                                        Dimension key
                                      type: string
                                    dimensionValue:
                                      description: |-
                                        (String) Dimension value
                                        Dimension value
                                      type: string
                                    operator:
                                      description: |-
                                        (String) Possible Values: CONTAINS_CASE_SENSITIVE, DOES_NOT_CONTAIN_CASE_SENSITIVE, DOES_NOT_EQUAL, DOES_NOT_START_WITH, EQUALS, STARTS_WITH
                                        Possible Values: `CONTAINS_CASE_SENSITIVE`, `DOES_NOT_CONTAIN_CASE_SENSITIVE`, `DOES_NOT_EQUAL`, `DOES_NOT_START_WITH`, `EQUALS`, `STARTS_WITH`
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                        entityFilter:
                          description: |-
                            based filters to define the scope this event monitors. (see below for nested schema)
                            Use rule-based filters to define the scope this event monitors.
                          items:
                            properties:
                              conditions:
                                description: |-
                                  (Block List, Max: 1) no documentation available (see below for nested schema)
                                  no documentation available
                                items:
                                  properties:
                                    condition:
                                      description: '(Block Set, Min: 1) (see below
                                        for nested schema)'
                                      items:
                                        properties:
                                          operator:
                                            description: |-
                                              (String) Possible Values: CONTAINS_CASE_SENSITIVE, DOES_NOT_CONTAIN_CASE_SENSITIVE, DOES_NOT_EQUAL, DOES_NOT_START_WITH, EQUALS, STARTS_WITH
                                              Possible Values: `CONTAINS_CASE_INSENSITIVE`, `CONTAINS_CASE_SENSITIVE`, `DOES_NOT_CONTAIN_CASE_INSENSITIVE`, `DOES_NOT_CONTAIN_CASE_SENSITIVE`, `DOES_NOT_EQUAL`, `DOES_NOT_START_WITH`, `EQUALS`, `STARTS_WITH`
                                            type: string
                                          type:
                                            description: |-
                                              (String) Possible Values: AUTO_ADAPTIVE_THRESHOLD, SEASONAL_BASELINE, STATIC_THRESHOLD
                                              Possible Values: `CUSTOM_DEVICE_GROUP_NAME`, `ENTITY_ID`, `HOST_GROUP_NAME`, `HOST_NAME`, `MANAGEMENT_ZONE`, `NAME`, `PROCESS_GROUP_ID`, `PROCESS_GROUP_NAME`, `TAG`
                                            type: string
                                          value:
                                            description: |-
                                              (String) no documentation available
                                              no documentation available
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              dimensionKey:
                                description: |-
                                  (String) Dimension key
                                  Dimension key of entity type to filter
                                type: string
                            type: object
                          type: array
                        managementZone:
                          description: |-
                            (String) The legacy_id of a Management Zone (as provided by the resource dynatrace_management_zone_v2 or the data source dynatrace_management_zone)
                            The `legacy_id` of a Management Zone (as provided by the resource `dynatrace_management_zone_v2` or the data source `dynatrace_management_zone`)
                          type: string
                        metricKey:
                          description: |-
                            (String) Metric key
                            Metric key
                          type: string
                        metricSelector:
                          description: |-
                            (String) To learn more, visit Metric Selector
                            To learn more, visit [Metric Selector](https://dt-url.net/metselad)
                          type: string
                        queryOffset:
                          description: |-
                            (Number) Minute offset of sliding evaluation window for metrics with latency
                            Minute offset of sliding evaluation window for metrics with latency
                          type: number
                        type:
                          description: |-
                            (String) Possible Values: AUTO_ADAPTIVE_THRESHOLD, SEASONAL_BASELINE, STATIC_THRESHOLD
                            Possible Values: `METRIC_KEY`, `METRIC_SELECTOR`
                          type: string
                      type: object
                    type: array
                  summary:
                    description: |-
                      (String) The textual summary of the metric event entry
                      The textual summary of the metric event entry
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
