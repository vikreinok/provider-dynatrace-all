---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: relationships.generic.dynatrace.crossplane.io
spec:
  group: generic.dynatrace.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: Relationships
    listKind: RelationshipsList
    plural: relationships
    singular: relationships
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Relationships is the Schema for the Relationshipss API. The resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RelationshipsSpec defines the desired state of Relationships
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  createdBy:
                    description: |-
                      (String) The user or extension that created this relationship.
                      The user or extension that created this relationship.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) This setting is enabled (true) or disabled (false)
                      This setting is enabled (`true`) or disabled (`false`)
                    type: boolean
                  fromRole:
                    description: |-
                      (String) Specify a role for the source entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the source type is considered for the relationship.
                      Specify a role for the source entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the source type is considered for the relationship.
                    type: string
                  fromType:
                    description: |-
                      (String) Define an entity type as the source of the relationship.
                      Define an entity type as the source of the relationship.
                    type: string
                  sources:
                    description: |-
                      (Block List, Min: 1, Max: 1) Specify all sources which should be evaluated for this relationship rule. The relationship is only created when any of the filters match. (see below for nested schema)
                      Specify all sources which should be evaluated for this relationship rule. The relationship is only created when any of the filters match.
                    items:
                      properties:
                        source:
                          description: '(Block Set, Min: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              condition:
                                description: |-
                                  (String) Specify a filter that needs to match in order for the extraction to happen.. Two different filters are supported: $eq(value) will ensure that the source matches exactly 'value', while $prefix(value) will ensure that the source begins with exactly 'value'.
                                  If your value contains the characters '(', ')' or '~', you need to escape them by adding a '~' in front of them.
                                  Specify a filter that needs to match in order for the extraction to happen.. Two different filters are supported: `$eq(value)` will ensure that the source matches exactly 'value', while `$prefix(value)` will ensure that the source begins with exactly 'value'.
                                  If your value contains the characters '(', ')' or '\~', you need to escape them by adding a '\~' in front of them.
                                type: string
                              mappingRules:
                                description: |-
                                  (Block List, Max: 1) Specify all properties which should be compared. If all mapping rules match a relationship between entities will be created. (see below for nested schema)
                                  Specify all properties which should be compared. If all mapping rules match a relationship between entities will be created.
                                items:
                                  properties:
                                    mappingRule:
                                      description: '(Block Set, Min: 1) (see below
                                        for nested schema)'
                                      items:
                                        properties:
                                          destinationProperty:
                                            description: |-
                                              sensitive name of a property of the destination type.
                                              The case-sensitive name of a property of the destination type.
                                            type: string
                                          destinationTransformation:
                                            description: |-
                                              is?. Possible Values: Leave text as-is, To lower case, To upper case
                                              Normalize text or leave it as-is?. Possible Values: `Leave text as-is`, `To lower case`, `To upper case`
                                            type: string
                                          sourceProperty:
                                            description: |-
                                              sensitive name of a property of the source type.
                                              The case-sensitive name of a property of the source type.
                                            type: string
                                          sourceTransformation:
                                            description: |-
                                              is?. Possible Values: Leave text as-is, To lower case, To upper case
                                              Normalize text or leave it as-is?. Possible Values: `Leave text as-is`, `To lower case`, `To upper case`
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              sourceType:
                                description: |-
                                  (String) Specify the source type of the filter to identify which data source should be evaluated.. Possible Values: Business Events, Entities, Events, Logs, Metrics, Spans, Topology
                                  Specify the source type of the filter to identify which data source should be evaluated.. Possible Values: `Business Events`, `Entities`, `Events`, `Logs`, `Metrics`, `Spans`, `Topology`
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  toRole:
                    description: |-
                      (String) Specify a role for the destination entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the destination type is considered for the relationship.
                      Specify a role for the destination entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the destination type is considered for the relationship.
                    type: string
                  toType:
                    description: |-
                      (String) Define an entity type as the destination of the relationship. You can choose the same type as the source type. In this case you also may assign different roles for source and destination for having directed relationships.
                      Define an entity type as the destination of the relationship. You can choose the same type as the source type. In this case you also may assign different roles for source and destination for having directed relationships.
                    type: string
                  typeOfRelation:
                    description: |-
                      (String) Type of the relationship between the Source Type and the Destination Type. Possible Values: CALLS, CHILD_OF, INSTANCE_OF, PART_OF, RUNS_ON, SAME_AS
                      Type of the relationship between the Source Type and the Destination Type. Possible Values: `CALLS`, `CHILD_OF`, `INSTANCE_OF`, `PART_OF`, `RUNS_ON`, `SAME_AS`
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  createdBy:
                    description: |-
                      (String) The user or extension that created this relationship.
                      The user or extension that created this relationship.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) This setting is enabled (true) or disabled (false)
                      This setting is enabled (`true`) or disabled (`false`)
                    type: boolean
                  fromRole:
                    description: |-
                      (String) Specify a role for the source entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the source type is considered for the relationship.
                      Specify a role for the source entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the source type is considered for the relationship.
                    type: string
                  fromType:
                    description: |-
                      (String) Define an entity type as the source of the relationship.
                      Define an entity type as the source of the relationship.
                    type: string
                  sources:
                    description: |-
                      (Block List, Min: 1, Max: 1) Specify all sources which should be evaluated for this relationship rule. The relationship is only created when any of the filters match. (see below for nested schema)
                      Specify all sources which should be evaluated for this relationship rule. The relationship is only created when any of the filters match.
                    items:
                      properties:
                        source:
                          description: '(Block Set, Min: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              condition:
                                description: |-
                                  (String) Specify a filter that needs to match in order for the extraction to happen.. Two different filters are supported: $eq(value) will ensure that the source matches exactly 'value', while $prefix(value) will ensure that the source begins with exactly 'value'.
                                  If your value contains the characters '(', ')' or '~', you need to escape them by adding a '~' in front of them.
                                  Specify a filter that needs to match in order for the extraction to happen.. Two different filters are supported: `$eq(value)` will ensure that the source matches exactly 'value', while `$prefix(value)` will ensure that the source begins with exactly 'value'.
                                  If your value contains the characters '(', ')' or '\~', you need to escape them by adding a '\~' in front of them.
                                type: string
                              mappingRules:
                                description: |-
                                  (Block List, Max: 1) Specify all properties which should be compared. If all mapping rules match a relationship between entities will be created. (see below for nested schema)
                                  Specify all properties which should be compared. If all mapping rules match a relationship between entities will be created.
                                items:
                                  properties:
                                    mappingRule:
                                      description: '(Block Set, Min: 1) (see below
                                        for nested schema)'
                                      items:
                                        properties:
                                          destinationProperty:
                                            description: |-
                                              sensitive name of a property of the destination type.
                                              The case-sensitive name of a property of the destination type.
                                            type: string
                                          destinationTransformation:
                                            description: |-
                                              is?. Possible Values: Leave text as-is, To lower case, To upper case
                                              Normalize text or leave it as-is?. Possible Values: `Leave text as-is`, `To lower case`, `To upper case`
                                            type: string
                                          sourceProperty:
                                            description: |-
                                              sensitive name of a property of the source type.
                                              The case-sensitive name of a property of the source type.
                                            type: string
                                          sourceTransformation:
                                            description: |-
                                              is?. Possible Values: Leave text as-is, To lower case, To upper case
                                              Normalize text or leave it as-is?. Possible Values: `Leave text as-is`, `To lower case`, `To upper case`
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              sourceType:
                                description: |-
                                  (String) Specify the source type of the filter to identify which data source should be evaluated.. Possible Values: Business Events, Entities, Events, Logs, Metrics, Spans, Topology
                                  Specify the source type of the filter to identify which data source should be evaluated.. Possible Values: `Business Events`, `Entities`, `Events`, `Logs`, `Metrics`, `Spans`, `Topology`
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  toRole:
                    description: |-
                      (String) Specify a role for the destination entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the destination type is considered for the relationship.
                      Specify a role for the destination entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the destination type is considered for the relationship.
                    type: string
                  toType:
                    description: |-
                      (String) Define an entity type as the destination of the relationship. You can choose the same type as the source type. In this case you also may assign different roles for source and destination for having directed relationships.
                      Define an entity type as the destination of the relationship. You can choose the same type as the source type. In this case you also may assign different roles for source and destination for having directed relationships.
                    type: string
                  typeOfRelation:
                    description: |-
                      (String) Type of the relationship between the Source Type and the Destination Type. Possible Values: CALLS, CHILD_OF, INSTANCE_OF, PART_OF, RUNS_ON, SAME_AS
                      Type of the relationship between the Source Type and the Destination Type. Possible Values: `CALLS`, `CHILD_OF`, `INSTANCE_OF`, `PART_OF`, `RUNS_ON`, `SAME_AS`
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.createdBy is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.createdBy)
                || (has(self.initProvider) && has(self.initProvider.createdBy))'
            - message: spec.forProvider.enabled is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enabled)
                || (has(self.initProvider) && has(self.initProvider.enabled))'
            - message: spec.forProvider.fromType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.fromType)
                || (has(self.initProvider) && has(self.initProvider.fromType))'
            - message: spec.forProvider.sources is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.sources)
                || (has(self.initProvider) && has(self.initProvider.sources))'
            - message: spec.forProvider.toType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.toType)
                || (has(self.initProvider) && has(self.initProvider.toType))'
            - message: spec.forProvider.typeOfRelation is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.typeOfRelation)
                || (has(self.initProvider) && has(self.initProvider.typeOfRelation))'
          status:
            description: RelationshipsStatus defines the observed state of Relationships.
            properties:
              atProvider:
                properties:
                  createdBy:
                    description: |-
                      (String) The user or extension that created this relationship.
                      The user or extension that created this relationship.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) This setting is enabled (true) or disabled (false)
                      This setting is enabled (`true`) or disabled (`false`)
                    type: boolean
                  fromRole:
                    description: |-
                      (String) Specify a role for the source entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the source type is considered for the relationship.
                      Specify a role for the source entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the source type is considered for the relationship.
                    type: string
                  fromType:
                    description: |-
                      (String) Define an entity type as the source of the relationship.
                      Define an entity type as the source of the relationship.
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  sources:
                    description: |-
                      (Block List, Min: 1, Max: 1) Specify all sources which should be evaluated for this relationship rule. The relationship is only created when any of the filters match. (see below for nested schema)
                      Specify all sources which should be evaluated for this relationship rule. The relationship is only created when any of the filters match.
                    items:
                      properties:
                        source:
                          description: '(Block Set, Min: 1) (see below for nested
                            schema)'
                          items:
                            properties:
                              condition:
                                description: |-
                                  (String) Specify a filter that needs to match in order for the extraction to happen.. Two different filters are supported: $eq(value) will ensure that the source matches exactly 'value', while $prefix(value) will ensure that the source begins with exactly 'value'.
                                  If your value contains the characters '(', ')' or '~', you need to escape them by adding a '~' in front of them.
                                  Specify a filter that needs to match in order for the extraction to happen.. Two different filters are supported: `$eq(value)` will ensure that the source matches exactly 'value', while `$prefix(value)` will ensure that the source begins with exactly 'value'.
                                  If your value contains the characters '(', ')' or '\~', you need to escape them by adding a '\~' in front of them.
                                type: string
                              mappingRules:
                                description: |-
                                  (Block List, Max: 1) Specify all properties which should be compared. If all mapping rules match a relationship between entities will be created. (see below for nested schema)
                                  Specify all properties which should be compared. If all mapping rules match a relationship between entities will be created.
                                items:
                                  properties:
                                    mappingRule:
                                      description: '(Block Set, Min: 1) (see below
                                        for nested schema)'
                                      items:
                                        properties:
                                          destinationProperty:
                                            description: |-
                                              sensitive name of a property of the destination type.
                                              The case-sensitive name of a property of the destination type.
                                            type: string
                                          destinationTransformation:
                                            description: |-
                                              is?. Possible Values: Leave text as-is, To lower case, To upper case
                                              Normalize text or leave it as-is?. Possible Values: `Leave text as-is`, `To lower case`, `To upper case`
                                            type: string
                                          sourceProperty:
                                            description: |-
                                              sensitive name of a property of the source type.
                                              The case-sensitive name of a property of the source type.
                                            type: string
                                          sourceTransformation:
                                            description: |-
                                              is?. Possible Values: Leave text as-is, To lower case, To upper case
                                              Normalize text or leave it as-is?. Possible Values: `Leave text as-is`, `To lower case`, `To upper case`
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              sourceType:
                                description: |-
                                  (String) Specify the source type of the filter to identify which data source should be evaluated.. Possible Values: Business Events, Entities, Events, Logs, Metrics, Spans, Topology
                                  Specify the source type of the filter to identify which data source should be evaluated.. Possible Values: `Business Events`, `Entities`, `Events`, `Logs`, `Metrics`, `Spans`, `Topology`
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  toRole:
                    description: |-
                      (String) Specify a role for the destination entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the destination type is considered for the relationship.
                      Specify a role for the destination entity. If both source and destination type are the same, referring different roles will allow identification of a relationships direction. If role is left blank, any role of the destination type is considered for the relationship.
                    type: string
                  toType:
                    description: |-
                      (String) Define an entity type as the destination of the relationship. You can choose the same type as the source type. In this case you also may assign different roles for source and destination for having directed relationships.
                      Define an entity type as the destination of the relationship. You can choose the same type as the source type. In this case you also may assign different roles for source and destination for having directed relationships.
                    type: string
                  typeOfRelation:
                    description: |-
                      (String) Type of the relationship between the Source Type and the Destination Type. Possible Values: CALLS, CHILD_OF, INSTANCE_OF, PART_OF, RUNS_ON, SAME_AS
                      Type of the relationship between the Source Type and the Destination Type. Possible Values: `CALLS`, `CHILD_OF`, `INSTANCE_OF`, `PART_OF`, `RUNS_ON`, `SAME_AS`
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
