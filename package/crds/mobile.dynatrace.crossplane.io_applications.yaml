---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: applications.mobile.dynatrace.crossplane.io
spec:
  group: mobile.dynatrace.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: Application
    listKind: ApplicationList
    plural: applications
    singular: application
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is the Schema for the Applications API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSpec defines the desired state of Application
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  apdex:
                    description: Apdex configuration of a mobile application. A duration
                      less than the **tolerable** threshold is considered satisfied
                    items:
                      properties:
                        frustrated:
                          description: 'Apdex **frustrated** threshold, in milliseconds:
                            a duration greater than or equal to this value is considered
                            frustrated'
                          type: number
                        frustratedOnError:
                          description: 'Apdex error condition: if `true` the user
                            session is considered frustrated when an error is reported'
                          type: boolean
                        tolerable:
                          description: 'Apdex **tolerable** threshold, in milliseconds:
                            a duration greater than or equal to this value is considered
                            tolerable'
                          type: number
                      type: object
                    type: array
                  applicationId:
                    description: |-
                      The UUID of the application.

                      It is used only by OneAgent to send data to Dynatrace. If not specified it will get generated.
                    type: string
                  applicationType:
                    description: The type of the application. Either `CUSTOM_APPLICATION`
                      or `MOBILE_APPLICATION`.
                    type: string
                  beaconEndpointType:
                    description: The type of the beacon endpoint. Possible values
                      are `CLUSTER_ACTIVE_GATE`, `ENVIRONMENT_ACTIVE_GATE` and `INSTRUMENTED_WEB_SERVER`.
                    type: string
                  beaconEndpointUrl:
                    description: |-
                      The URL of the beacon endpoint.

                      Only applicable when the **beacon_endpoint_type** is set to `ENVIRONMENT_ACTIVE_GATE` or `INSTRUMENTED_WEB_SERVER`
                    type: string
                  iconType:
                    description: Custom application icon. Mobile apps always use the
                      mobile device icon, so this icon can only be set for custom
                      apps.
                    type: string
                  keyUserActions:
                    description: User Action names to be flagged as Key User Actions
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: The name of the application
                    type: string
                  optInMode:
                    description: The opt-in mode is enabled (`true`) or disabled (`false`)
                    type: boolean
                  properties:
                    description: User Action and Session Properties
                    items:
                      properties:
                        apiValue:
                          description: A User Action / Session Property based on a
                            value reported by the API
                          items:
                            properties:
                              aggregation:
                                description: The aggregation type of the property.
                                  It defines how multiple values of the property are
                                  aggregated. Possible values are `SUM`, `MIN`, `MAX`,
                                  `FIRST` and `LAST`
                                type: string
                              cleanupRule:
                                description: The cleanup rule of the property. Defines
                                  how to extract the data you need from a string value.
                                  Specify the [regular expression](https://dt-url.net/k9e0iaq)
                                  for the data you need there
                                type: string
                              displayName:
                                description: The display name of the property
                                type: string
                              key:
                                description: The unique key of the mobile session
                                  or user action property
                                type: string
                              name:
                                description: The name of the reported value
                                type: string
                              storeAsSessionProperty:
                                description: If `true`, the property is stored as
                                  a session property
                                type: boolean
                              storeAsUserActionProperty:
                                description: If `true`, the property is stored as
                                  a user action property
                                type: boolean
                              type:
                                description: The data type of the property. Possible
                                  values are `DOUBLE`, `LONG` and `STRING`
                                type: string
                            type: object
                          type: array
                        requestAttribute:
                          description: A User Action / Session Property based on a
                            Server Side Request Attribute
                          items:
                            properties:
                              aggregation:
                                description: The aggregation type of the property.
                                  It defines how multiple values of the property are
                                  aggregated. Possible values are `SUM`, `MIN`, `MAX`,
                                  `FIRST` and `LAST`
                                type: string
                              cleanupRule:
                                description: The cleanup rule of the property. Defines
                                  how to extract the data you need from a string value.
                                  Specify the [regular expression](https://dt-url.net/k9e0iaq)
                                  for the data you need there
                                type: string
                              displayName:
                                description: The display name of the property
                                type: string
                              id:
                                description: The ID of the request attribute
                                type: string
                              key:
                                description: The unique key of the mobile session
                                  or user action property
                                type: string
                              storeAsSessionProperty:
                                description: If `true`, the property is stored as
                                  a session property
                                type: boolean
                              storeAsUserActionProperty:
                                description: If `true`, the property is stored as
                                  a user action property
                                type: boolean
                              type:
                                description: The data type of the property. Possible
                                  values are `DOUBLE`, `LONG` and `STRING`. The value
                                  MUST match the data type of the Request Attribute.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  sessionReplay:
                    description: (Field has overlap with `dynatrace_mobile_app_enablement`)
                      The session replay is enabled (`true`) or disabled (`false`).
                    type: boolean
                  sessionReplayOnCrash:
                    description: |-
                      The session replay on crash is enabled (`true`) or disabled (`false`).

                      Enabling requires both **sessionReplayEnabled** and **optInModeEnabled** values set to `true`.
                    type: boolean
                  userSessionPercentage:
                    description: (Field has overlap with `dynatrace_mobile_app_enablement`
                      for mobile and `dynatrace_custom_app_enablement` for custom
                      apps) The percentage of user sessions to be analyzed
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  apdex:
                    description: Apdex configuration of a mobile application. A duration
                      less than the **tolerable** threshold is considered satisfied
                    items:
                      properties:
                        frustrated:
                          description: 'Apdex **frustrated** threshold, in milliseconds:
                            a duration greater than or equal to this value is considered
                            frustrated'
                          type: number
                        frustratedOnError:
                          description: 'Apdex error condition: if `true` the user
                            session is considered frustrated when an error is reported'
                          type: boolean
                        tolerable:
                          description: 'Apdex **tolerable** threshold, in milliseconds:
                            a duration greater than or equal to this value is considered
                            tolerable'
                          type: number
                      type: object
                    type: array
                  applicationId:
                    description: |-
                      The UUID of the application.

                      It is used only by OneAgent to send data to Dynatrace. If not specified it will get generated.
                    type: string
                  applicationType:
                    description: The type of the application. Either `CUSTOM_APPLICATION`
                      or `MOBILE_APPLICATION`.
                    type: string
                  beaconEndpointType:
                    description: The type of the beacon endpoint. Possible values
                      are `CLUSTER_ACTIVE_GATE`, `ENVIRONMENT_ACTIVE_GATE` and `INSTRUMENTED_WEB_SERVER`.
                    type: string
                  beaconEndpointUrl:
                    description: |-
                      The URL of the beacon endpoint.

                      Only applicable when the **beacon_endpoint_type** is set to `ENVIRONMENT_ACTIVE_GATE` or `INSTRUMENTED_WEB_SERVER`
                    type: string
                  iconType:
                    description: Custom application icon. Mobile apps always use the
                      mobile device icon, so this icon can only be set for custom
                      apps.
                    type: string
                  keyUserActions:
                    description: User Action names to be flagged as Key User Actions
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: The name of the application
                    type: string
                  optInMode:
                    description: The opt-in mode is enabled (`true`) or disabled (`false`)
                    type: boolean
                  properties:
                    description: User Action and Session Properties
                    items:
                      properties:
                        apiValue:
                          description: A User Action / Session Property based on a
                            value reported by the API
                          items:
                            properties:
                              aggregation:
                                description: The aggregation type of the property.
                                  It defines how multiple values of the property are
                                  aggregated. Possible values are `SUM`, `MIN`, `MAX`,
                                  `FIRST` and `LAST`
                                type: string
                              cleanupRule:
                                description: The cleanup rule of the property. Defines
                                  how to extract the data you need from a string value.
                                  Specify the [regular expression](https://dt-url.net/k9e0iaq)
                                  for the data you need there
                                type: string
                              displayName:
                                description: The display name of the property
                                type: string
                              key:
                                description: The unique key of the mobile session
                                  or user action property
                                type: string
                              name:
                                description: The name of the reported value
                                type: string
                              storeAsSessionProperty:
                                description: If `true`, the property is stored as
                                  a session property
                                type: boolean
                              storeAsUserActionProperty:
                                description: If `true`, the property is stored as
                                  a user action property
                                type: boolean
                              type:
                                description: The data type of the property. Possible
                                  values are `DOUBLE`, `LONG` and `STRING`
                                type: string
                            type: object
                          type: array
                        requestAttribute:
                          description: A User Action / Session Property based on a
                            Server Side Request Attribute
                          items:
                            properties:
                              aggregation:
                                description: The aggregation type of the property.
                                  It defines how multiple values of the property are
                                  aggregated. Possible values are `SUM`, `MIN`, `MAX`,
                                  `FIRST` and `LAST`
                                type: string
                              cleanupRule:
                                description: The cleanup rule of the property. Defines
                                  how to extract the data you need from a string value.
                                  Specify the [regular expression](https://dt-url.net/k9e0iaq)
                                  for the data you need there
                                type: string
                              displayName:
                                description: The display name of the property
                                type: string
                              id:
                                description: The ID of the request attribute
                                type: string
                              key:
                                description: The unique key of the mobile session
                                  or user action property
                                type: string
                              storeAsSessionProperty:
                                description: If `true`, the property is stored as
                                  a session property
                                type: boolean
                              storeAsUserActionProperty:
                                description: If `true`, the property is stored as
                                  a user action property
                                type: boolean
                              type:
                                description: The data type of the property. Possible
                                  values are `DOUBLE`, `LONG` and `STRING`. The value
                                  MUST match the data type of the Request Attribute.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  sessionReplay:
                    description: (Field has overlap with `dynatrace_mobile_app_enablement`)
                      The session replay is enabled (`true`) or disabled (`false`).
                    type: boolean
                  sessionReplayOnCrash:
                    description: |-
                      The session replay on crash is enabled (`true`) or disabled (`false`).

                      Enabling requires both **sessionReplayEnabled** and **optInModeEnabled** values set to `true`.
                    type: boolean
                  userSessionPercentage:
                    description: (Field has overlap with `dynatrace_mobile_app_enablement`
                      for mobile and `dynatrace_custom_app_enablement` for custom
                      apps) The percentage of user sessions to be analyzed
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.apdex is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.apdex)
                || (has(self.initProvider) && has(self.initProvider.apdex))'
            - message: spec.forProvider.beaconEndpointType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.beaconEndpointType)
                || (has(self.initProvider) && has(self.initProvider.beaconEndpointType))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ApplicationStatus defines the observed state of Application.
            properties:
              atProvider:
                properties:
                  apdex:
                    description: Apdex configuration of a mobile application. A duration
                      less than the **tolerable** threshold is considered satisfied
                    items:
                      properties:
                        frustrated:
                          description: 'Apdex **frustrated** threshold, in milliseconds:
                            a duration greater than or equal to this value is considered
                            frustrated'
                          type: number
                        frustratedOnError:
                          description: 'Apdex error condition: if `true` the user
                            session is considered frustrated when an error is reported'
                          type: boolean
                        tolerable:
                          description: 'Apdex **tolerable** threshold, in milliseconds:
                            a duration greater than or equal to this value is considered
                            tolerable'
                          type: number
                      type: object
                    type: array
                  applicationId:
                    description: |-
                      The UUID of the application.

                      It is used only by OneAgent to send data to Dynatrace. If not specified it will get generated.
                    type: string
                  applicationType:
                    description: The type of the application. Either `CUSTOM_APPLICATION`
                      or `MOBILE_APPLICATION`.
                    type: string
                  beaconEndpointType:
                    description: The type of the beacon endpoint. Possible values
                      are `CLUSTER_ACTIVE_GATE`, `ENVIRONMENT_ACTIVE_GATE` and `INSTRUMENTED_WEB_SERVER`.
                    type: string
                  beaconEndpointUrl:
                    description: |-
                      The URL of the beacon endpoint.

                      Only applicable when the **beacon_endpoint_type** is set to `ENVIRONMENT_ACTIVE_GATE` or `INSTRUMENTED_WEB_SERVER`
                    type: string
                  iconType:
                    description: Custom application icon. Mobile apps always use the
                      mobile device icon, so this icon can only be set for custom
                      apps.
                    type: string
                  id:
                    type: string
                  keyUserActions:
                    description: User Action names to be flagged as Key User Actions
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: The name of the application
                    type: string
                  optInMode:
                    description: The opt-in mode is enabled (`true`) or disabled (`false`)
                    type: boolean
                  properties:
                    description: User Action and Session Properties
                    items:
                      properties:
                        apiValue:
                          description: A User Action / Session Property based on a
                            value reported by the API
                          items:
                            properties:
                              aggregation:
                                description: The aggregation type of the property.
                                  It defines how multiple values of the property are
                                  aggregated. Possible values are `SUM`, `MIN`, `MAX`,
                                  `FIRST` and `LAST`
                                type: string
                              cleanupRule:
                                description: The cleanup rule of the property. Defines
                                  how to extract the data you need from a string value.
                                  Specify the [regular expression](https://dt-url.net/k9e0iaq)
                                  for the data you need there
                                type: string
                              displayName:
                                description: The display name of the property
                                type: string
                              key:
                                description: The unique key of the mobile session
                                  or user action property
                                type: string
                              name:
                                description: The name of the reported value
                                type: string
                              storeAsSessionProperty:
                                description: If `true`, the property is stored as
                                  a session property
                                type: boolean
                              storeAsUserActionProperty:
                                description: If `true`, the property is stored as
                                  a user action property
                                type: boolean
                              type:
                                description: The data type of the property. Possible
                                  values are `DOUBLE`, `LONG` and `STRING`
                                type: string
                            type: object
                          type: array
                        requestAttribute:
                          description: A User Action / Session Property based on a
                            Server Side Request Attribute
                          items:
                            properties:
                              aggregation:
                                description: The aggregation type of the property.
                                  It defines how multiple values of the property are
                                  aggregated. Possible values are `SUM`, `MIN`, `MAX`,
                                  `FIRST` and `LAST`
                                type: string
                              cleanupRule:
                                description: The cleanup rule of the property. Defines
                                  how to extract the data you need from a string value.
                                  Specify the [regular expression](https://dt-url.net/k9e0iaq)
                                  for the data you need there
                                type: string
                              displayName:
                                description: The display name of the property
                                type: string
                              id:
                                description: The ID of the request attribute
                                type: string
                              key:
                                description: The unique key of the mobile session
                                  or user action property
                                type: string
                              storeAsSessionProperty:
                                description: If `true`, the property is stored as
                                  a session property
                                type: boolean
                              storeAsUserActionProperty:
                                description: If `true`, the property is stored as
                                  a user action property
                                type: boolean
                              type:
                                description: The data type of the property. Possible
                                  values are `DOUBLE`, `LONG` and `STRING`. The value
                                  MUST match the data type of the Request Attribute.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  sessionReplay:
                    description: (Field has overlap with `dynatrace_mobile_app_enablement`)
                      The session replay is enabled (`true`) or disabled (`false`).
                    type: boolean
                  sessionReplayOnCrash:
                    description: |-
                      The session replay on crash is enabled (`true`) or disabled (`false`).

                      Enabling requires both **sessionReplayEnabled** and **optInModeEnabled** values set to `true`.
                    type: boolean
                  userSessionPercentage:
                    description: (Field has overlap with `dynatrace_mobile_app_enablement`
                      for mobile and `dynatrace_custom_app_enablement` for custom
                      apps) The percentage of user sessions to be analyzed
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
