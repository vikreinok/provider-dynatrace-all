---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: dataprivacies.application.dynatrace.m.crossplane.io
spec:
  group: application.dynatrace.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: DataPrivacy
    listKind: DataPrivacyList
    plural: dataprivacies
    singular: dataprivacy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataPrivacy is the Schema for the DataPrivacys API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataPrivacySpec defines the desired state of DataPrivacy
            properties:
              forProvider:
                properties:
                  dataCaptureOptIn:
                    description: (Field has overlap with `dynatrace_data_privacy`)
                      Set to `true` to disable data capture and cookies until JavaScriptAPI
                      `dtrum.enable()` is called
                    type: boolean
                  doNotTrackBehaviour:
                    description: |-
                      (Field has overlap with `dynatrace_data_privacy`) How to handle the "Do Not Track" header:

                      * `IGNORE_DO_NOT_TRACK`: ignore the header and capture the data.
                      * `CAPTURE_ANONYMIZED`: capture the data but do not tie it to the user.
                      * `DO_NOT_CAPTURE`: respect the header and do not capture.
                    type: string
                  persistentCookieForUserTracking:
                    description: (Field has overlap with `dynatrace_data_privacy`)
                      Set to `true` to set persistent cookie in order to recognize
                      returning devices
                    type: boolean
                  sessionReplayDataPrivacy:
                    description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                      Data privacy settings for Session Replay
                    items:
                      properties:
                        contentMaskingSettings:
                          description: |-
                            (Field has overlap with `dynatrace_session_replay_web_privacy`) Content masking settings for Session Replay.

                            For more details, see [Configure Session Replay](https://dt-url.net/0m03slq) in Dynatrace Documentation
                          items:
                            properties:
                              playback:
                                description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                                  Configuration of the Session Replay masking during
                                  Playback
                                items:
                                  properties:
                                    preset:
                                      description: |-
                                        The type of the masking:

                                        * `MASK_ALL`: Mask all texts, user input, and images.
                                        * `MASK_USER_INPUT`: Mask all data that is provided through user input
                                        * `ALLOW_LIST`: Only elements, specified in **maskingRules** are shown, everything else is masked.
                                        * `BLOCK_LIST`: Elements, specified in **maskingRules** are masked, everything else is shown
                                      type: string
                                    rules:
                                      description: A list of masking rules
                                      items:
                                        properties:
                                          rule:
                                            description: The masking rule defining
                                              how data is hidden
                                            items:
                                              properties:
                                                selector:
                                                  description: |-
                                                    The selector for the element or the attribute to be masked.

                                                    Specify a CSS expression for an element or a [regular expression](https://dt-url.net/k9e0iaq) for an attribute
                                                  type: string
                                                type:
                                                  description: The type of the masking
                                                    rule
                                                  type: string
                                                userInteractionHidden:
                                                  description: Interactions with the
                                                    element are (`true`) or are not
                                                    (`false) masked
                                                  type: boolean
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              recording:
                                description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                                  Configuration of the Session Replay masking during
                                  Recording
                                items:
                                  properties:
                                    preset:
                                      description: |-
                                        The type of the masking:

                                        * `MASK_ALL`: Mask all texts, user input, and images.
                                        * `MASK_USER_INPUT`: Mask all data that is provided through user input
                                        * `ALLOW_LIST`: Only elements, specified in **maskingRules** are shown, everything else is masked.
                                        * `BLOCK_LIST`: Elements, specified in **maskingRules** are masked, everything else is shown
                                      type: string
                                    rules:
                                      description: A list of masking rules
                                      items:
                                        properties:
                                          rule:
                                            description: The masking rule defining
                                              how data is hidden
                                            items:
                                              properties:
                                                selector:
                                                  description: |-
                                                    The selector for the element or the attribute to be masked.

                                                    Specify a CSS expression for an element or a [regular expression](https://dt-url.net/k9e0iaq) for an attribute
                                                  type: string
                                                type:
                                                  description: The type of the masking
                                                    rule
                                                  type: string
                                                userInteractionHidden:
                                                  description: Interactions with the
                                                    element are (`true`) or are not
                                                    (`false) masked
                                                  type: boolean
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        optIn:
                          description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                            If `true`, session recording is disabled until JavaScriptAPI
                            `dtrum.enableSessionReplay()` is called
                          type: boolean
                        urlExclusionRules:
                          description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                            A list of URLs to be excluded from recording
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  webApplicationId:
                    description: Dynatrace entity ID of the web application
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  dataCaptureOptIn:
                    description: (Field has overlap with `dynatrace_data_privacy`)
                      Set to `true` to disable data capture and cookies until JavaScriptAPI
                      `dtrum.enable()` is called
                    type: boolean
                  doNotTrackBehaviour:
                    description: |-
                      (Field has overlap with `dynatrace_data_privacy`) How to handle the "Do Not Track" header:

                      * `IGNORE_DO_NOT_TRACK`: ignore the header and capture the data.
                      * `CAPTURE_ANONYMIZED`: capture the data but do not tie it to the user.
                      * `DO_NOT_CAPTURE`: respect the header and do not capture.
                    type: string
                  persistentCookieForUserTracking:
                    description: (Field has overlap with `dynatrace_data_privacy`)
                      Set to `true` to set persistent cookie in order to recognize
                      returning devices
                    type: boolean
                  sessionReplayDataPrivacy:
                    description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                      Data privacy settings for Session Replay
                    items:
                      properties:
                        contentMaskingSettings:
                          description: |-
                            (Field has overlap with `dynatrace_session_replay_web_privacy`) Content masking settings for Session Replay.

                            For more details, see [Configure Session Replay](https://dt-url.net/0m03slq) in Dynatrace Documentation
                          items:
                            properties:
                              playback:
                                description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                                  Configuration of the Session Replay masking during
                                  Playback
                                items:
                                  properties:
                                    preset:
                                      description: |-
                                        The type of the masking:

                                        * `MASK_ALL`: Mask all texts, user input, and images.
                                        * `MASK_USER_INPUT`: Mask all data that is provided through user input
                                        * `ALLOW_LIST`: Only elements, specified in **maskingRules** are shown, everything else is masked.
                                        * `BLOCK_LIST`: Elements, specified in **maskingRules** are masked, everything else is shown
                                      type: string
                                    rules:
                                      description: A list of masking rules
                                      items:
                                        properties:
                                          rule:
                                            description: The masking rule defining
                                              how data is hidden
                                            items:
                                              properties:
                                                selector:
                                                  description: |-
                                                    The selector for the element or the attribute to be masked.

                                                    Specify a CSS expression for an element or a [regular expression](https://dt-url.net/k9e0iaq) for an attribute
                                                  type: string
                                                type:
                                                  description: The type of the masking
                                                    rule
                                                  type: string
                                                userInteractionHidden:
                                                  description: Interactions with the
                                                    element are (`true`) or are not
                                                    (`false) masked
                                                  type: boolean
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              recording:
                                description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                                  Configuration of the Session Replay masking during
                                  Recording
                                items:
                                  properties:
                                    preset:
                                      description: |-
                                        The type of the masking:

                                        * `MASK_ALL`: Mask all texts, user input, and images.
                                        * `MASK_USER_INPUT`: Mask all data that is provided through user input
                                        * `ALLOW_LIST`: Only elements, specified in **maskingRules** are shown, everything else is masked.
                                        * `BLOCK_LIST`: Elements, specified in **maskingRules** are masked, everything else is shown
                                      type: string
                                    rules:
                                      description: A list of masking rules
                                      items:
                                        properties:
                                          rule:
                                            description: The masking rule defining
                                              how data is hidden
                                            items:
                                              properties:
                                                selector:
                                                  description: |-
                                                    The selector for the element or the attribute to be masked.

                                                    Specify a CSS expression for an element or a [regular expression](https://dt-url.net/k9e0iaq) for an attribute
                                                  type: string
                                                type:
                                                  description: The type of the masking
                                                    rule
                                                  type: string
                                                userInteractionHidden:
                                                  description: Interactions with the
                                                    element are (`true`) or are not
                                                    (`false) masked
                                                  type: boolean
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        optIn:
                          description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                            If `true`, session recording is disabled until JavaScriptAPI
                            `dtrum.enableSessionReplay()` is called
                          type: boolean
                        urlExclusionRules:
                          description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                            A list of URLs to be excluded from recording
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  webApplicationId:
                    description: Dynatrace entity ID of the web application
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.doNotTrackBehaviour is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.doNotTrackBehaviour)
                || (has(self.initProvider) && has(self.initProvider.doNotTrackBehaviour))'
            - message: spec.forProvider.sessionReplayDataPrivacy is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.sessionReplayDataPrivacy)
                || (has(self.initProvider) && has(self.initProvider.sessionReplayDataPrivacy))'
            - message: spec.forProvider.webApplicationId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.webApplicationId)
                || (has(self.initProvider) && has(self.initProvider.webApplicationId))'
          status:
            description: DataPrivacyStatus defines the observed state of DataPrivacy.
            properties:
              atProvider:
                properties:
                  dataCaptureOptIn:
                    description: (Field has overlap with `dynatrace_data_privacy`)
                      Set to `true` to disable data capture and cookies until JavaScriptAPI
                      `dtrum.enable()` is called
                    type: boolean
                  doNotTrackBehaviour:
                    description: |-
                      (Field has overlap with `dynatrace_data_privacy`) How to handle the "Do Not Track" header:

                      * `IGNORE_DO_NOT_TRACK`: ignore the header and capture the data.
                      * `CAPTURE_ANONYMIZED`: capture the data but do not tie it to the user.
                      * `DO_NOT_CAPTURE`: respect the header and do not capture.
                    type: string
                  id:
                    type: string
                  persistentCookieForUserTracking:
                    description: (Field has overlap with `dynatrace_data_privacy`)
                      Set to `true` to set persistent cookie in order to recognize
                      returning devices
                    type: boolean
                  sessionReplayDataPrivacy:
                    description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                      Data privacy settings for Session Replay
                    items:
                      properties:
                        contentMaskingSettings:
                          description: |-
                            (Field has overlap with `dynatrace_session_replay_web_privacy`) Content masking settings for Session Replay.

                            For more details, see [Configure Session Replay](https://dt-url.net/0m03slq) in Dynatrace Documentation
                          items:
                            properties:
                              playback:
                                description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                                  Configuration of the Session Replay masking during
                                  Playback
                                items:
                                  properties:
                                    preset:
                                      description: |-
                                        The type of the masking:

                                        * `MASK_ALL`: Mask all texts, user input, and images.
                                        * `MASK_USER_INPUT`: Mask all data that is provided through user input
                                        * `ALLOW_LIST`: Only elements, specified in **maskingRules** are shown, everything else is masked.
                                        * `BLOCK_LIST`: Elements, specified in **maskingRules** are masked, everything else is shown
                                      type: string
                                    rules:
                                      description: A list of masking rules
                                      items:
                                        properties:
                                          rule:
                                            description: The masking rule defining
                                              how data is hidden
                                            items:
                                              properties:
                                                selector:
                                                  description: |-
                                                    The selector for the element or the attribute to be masked.

                                                    Specify a CSS expression for an element or a [regular expression](https://dt-url.net/k9e0iaq) for an attribute
                                                  type: string
                                                type:
                                                  description: The type of the masking
                                                    rule
                                                  type: string
                                                userInteractionHidden:
                                                  description: Interactions with the
                                                    element are (`true`) or are not
                                                    (`false) masked
                                                  type: boolean
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              recording:
                                description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                                  Configuration of the Session Replay masking during
                                  Recording
                                items:
                                  properties:
                                    preset:
                                      description: |-
                                        The type of the masking:

                                        * `MASK_ALL`: Mask all texts, user input, and images.
                                        * `MASK_USER_INPUT`: Mask all data that is provided through user input
                                        * `ALLOW_LIST`: Only elements, specified in **maskingRules** are shown, everything else is masked.
                                        * `BLOCK_LIST`: Elements, specified in **maskingRules** are masked, everything else is shown
                                      type: string
                                    rules:
                                      description: A list of masking rules
                                      items:
                                        properties:
                                          rule:
                                            description: The masking rule defining
                                              how data is hidden
                                            items:
                                              properties:
                                                selector:
                                                  description: |-
                                                    The selector for the element or the attribute to be masked.

                                                    Specify a CSS expression for an element or a [regular expression](https://dt-url.net/k9e0iaq) for an attribute
                                                  type: string
                                                type:
                                                  description: The type of the masking
                                                    rule
                                                  type: string
                                                userInteractionHidden:
                                                  description: Interactions with the
                                                    element are (`true`) or are not
                                                    (`false) masked
                                                  type: boolean
                                              type: object
                                            type: array
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        optIn:
                          description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                            If `true`, session recording is disabled until JavaScriptAPI
                            `dtrum.enableSessionReplay()` is called
                          type: boolean
                        urlExclusionRules:
                          description: (Field has overlap with `dynatrace_session_replay_web_privacy`)
                            A list of URLs to be excluded from recording
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  webApplicationId:
                    description: Dynatrace entity ID of the web application
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
