---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: mobilemetrics.calculated.dynatrace.crossplane.io
spec:
  group: calculated.dynatrace.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: MobileMetric
    listKind: MobileMetricList
    plural: mobilemetrics
    singular: mobilemetric
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MobileMetric is the Schema for the MobileMetrics API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MobileMetricSpec defines the desired state of MobileMetric
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  appIdentifier:
                    description: The Dynatrace entity ID of the application to which
                      the metric belongs.
                    type: string
                  description:
                    description: Descriptor of a calculated mobile/custom app metric.
                    type: string
                  dimensions:
                    description: Parameters of a definition of a calculated mobile/custom
                      app metric.
                    items:
                      properties:
                        dimension:
                          description: A dimensions for the metric usage
                          items:
                            properties:
                              dimension:
                                description: The dimension of the metric. Possible
                                  values are `ApdexType`, `Browser`, `ErrorContext`,
                                  `ErrorOrigin`, `ErrorType`, `GeoLocation`, `StringProperty`,
                                  `UserActionType`
                                type: string
                              topX:
                                description: The number of top values to be calculated
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  enabled:
                    description: The metric is enabled (`true`) or disabled (`false`)
                    type: boolean
                  metricKey:
                    description: The unique key of the calculated mobile/custom app
                      metric.
                    type: string
                  metricType:
                    description: 'The type of the metric. Possible values: [ REPORTED_ERROR_COUNT,
                      USER_ACTION_DURATION, WEB_REQUEST_COUNT, WEB_REQUEST_ERROR_COUNT
                      ]'
                    type: string
                  name:
                    description: The displayed name of the metric.
                    type: string
                  userActionFilter:
                    description: Parameters of a definition of a calculated mobile/custom
                      app metric.
                    items:
                      properties:
                        actionDurationFromMilliseconds:
                          description: Only actions with a duration more than or equal
                            to this value (in milliseconds) are included in the metric
                            calculation.
                          type: number
                        actionDurationToMilliseconds:
                          description: Only actions with a duration less than or equal
                            to this value (in milliseconds) are included in the metric
                            calculation.
                          type: number
                        apdex:
                          description: 'Only actions with the specified Apdex score
                            are included in the metric calculation. Possible values:
                            [ Frustrated, Satisfied, Tolerating, Unknown ]'
                          type: string
                        appVersion:
                          description: Only actions coming from this app version are
                            included in the metric calculation.
                          type: string
                        carrier:
                          description: Only actions coming from this carrier type
                            are included in the metric calculation.
                          type: string
                        city:
                          description: Only actions of users from this city are included
                            in the metric calculation. Specify geolocation ID here.
                          type: string
                        connectionType:
                          description: 'Only actions coming from this connection type
                            are included in the metric calculation. Possible values:
                            [ LAN, MOBILE, OFFLINE, UNKNOWN, WIFI ]'
                          type: string
                        continent:
                          description: Only actions of users from this continent are
                            included in the metric calculation. Specify geolocation
                            ID here.
                          type: string
                        country:
                          description: Only actions of users from this country are
                            included in the metric calculation. Specify geolocation
                            ID here.
                          type: string
                        device:
                          description: Only actions coming from this app version are
                            included in the metric calculation.
                          type: string
                        hasHttpError:
                          description: 'The request error status of the actions to
                            be included in the metric calculation: `true` or `false`'
                          type: boolean
                        hasReportedError:
                          description: 'The error status of the actions to be included
                            in the metric calculation: `true` or `false`'
                          type: boolean
                        isp:
                          description: Only actions coming from this internet service
                            provider are included in the metric calculation.
                          type: string
                        manufacturer:
                          description: Only actions coming from devices of this manufacturer
                            are included in the metric calculation.
                          type: string
                        networkTechnology:
                          description: Filter by network technology
                          type: string
                        orientation:
                          description: 'Only actions coming from devices with this
                            display orientation are included in the metric calculation.
                            Possible values: [ LANDSCAPE, PORTRAIT, UNKNOWN ]'
                          type: string
                        osFamily:
                          description: Only actions coming from this OS family are
                            included in the metric calculation.
                          type: string
                        osVersion:
                          description: Only actions coming from this OS version are
                            included in the metric calculation.
                          type: string
                        region:
                          description: Only actions of users from this region are
                            included in the metric calculation. Specify geolocation
                            ID here.
                          type: string
                        resolution:
                          description: 'Only actions coming from devices with this
                            display resolution are included in the metric calculation.
                            Possible values: [ CGA, DCI2K, DCI4K, DVGA, FHD, FWVGA,
                            FWXGA, GHDPlus, HD, HQVGA, HQVGA2, HSXGA, HUXGA, HVGA,
                            HXGA, NTSC, PAL, QHD, QQVGA, QSXGA, QUXGA, QVGA, QWXGA,
                            QXGA, SVGA, SXGA, SXGAMinus, SXGAPlus, UGA, UHD16K, UHD4K,
                            UHD8K, UHDPlus, UNKNOWN, UWQHD, UXGA, VGA, WHSXGA, WHUXGA,
                            WHXGA, WQSXGA, WQUXGA, WQVGA, WQVGA2, WQVGA3, WQXGA, WQXGA2,
                            WSVGA, WSVGA2, WSXGA, WSXGAPlus, WUXGA, WVGA, WVGA2, WXGA,
                            WXGA2, WXGA3, WXGAPlus, XGA, XGAPLUS, _1280x854, nHD,
                            qHD ]'
                          type: string
                        userActionName:
                          description: Only actions with this name are included in
                            the metric calculation.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  appIdentifier:
                    description: The Dynatrace entity ID of the application to which
                      the metric belongs.
                    type: string
                  description:
                    description: Descriptor of a calculated mobile/custom app metric.
                    type: string
                  dimensions:
                    description: Parameters of a definition of a calculated mobile/custom
                      app metric.
                    items:
                      properties:
                        dimension:
                          description: A dimensions for the metric usage
                          items:
                            properties:
                              dimension:
                                description: The dimension of the metric. Possible
                                  values are `ApdexType`, `Browser`, `ErrorContext`,
                                  `ErrorOrigin`, `ErrorType`, `GeoLocation`, `StringProperty`,
                                  `UserActionType`
                                type: string
                              topX:
                                description: The number of top values to be calculated
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  enabled:
                    description: The metric is enabled (`true`) or disabled (`false`)
                    type: boolean
                  metricKey:
                    description: The unique key of the calculated mobile/custom app
                      metric.
                    type: string
                  metricType:
                    description: 'The type of the metric. Possible values: [ REPORTED_ERROR_COUNT,
                      USER_ACTION_DURATION, WEB_REQUEST_COUNT, WEB_REQUEST_ERROR_COUNT
                      ]'
                    type: string
                  name:
                    description: The displayed name of the metric.
                    type: string
                  userActionFilter:
                    description: Parameters of a definition of a calculated mobile/custom
                      app metric.
                    items:
                      properties:
                        actionDurationFromMilliseconds:
                          description: Only actions with a duration more than or equal
                            to this value (in milliseconds) are included in the metric
                            calculation.
                          type: number
                        actionDurationToMilliseconds:
                          description: Only actions with a duration less than or equal
                            to this value (in milliseconds) are included in the metric
                            calculation.
                          type: number
                        apdex:
                          description: 'Only actions with the specified Apdex score
                            are included in the metric calculation. Possible values:
                            [ Frustrated, Satisfied, Tolerating, Unknown ]'
                          type: string
                        appVersion:
                          description: Only actions coming from this app version are
                            included in the metric calculation.
                          type: string
                        carrier:
                          description: Only actions coming from this carrier type
                            are included in the metric calculation.
                          type: string
                        city:
                          description: Only actions of users from this city are included
                            in the metric calculation. Specify geolocation ID here.
                          type: string
                        connectionType:
                          description: 'Only actions coming from this connection type
                            are included in the metric calculation. Possible values:
                            [ LAN, MOBILE, OFFLINE, UNKNOWN, WIFI ]'
                          type: string
                        continent:
                          description: Only actions of users from this continent are
                            included in the metric calculation. Specify geolocation
                            ID here.
                          type: string
                        country:
                          description: Only actions of users from this country are
                            included in the metric calculation. Specify geolocation
                            ID here.
                          type: string
                        device:
                          description: Only actions coming from this app version are
                            included in the metric calculation.
                          type: string
                        hasHttpError:
                          description: 'The request error status of the actions to
                            be included in the metric calculation: `true` or `false`'
                          type: boolean
                        hasReportedError:
                          description: 'The error status of the actions to be included
                            in the metric calculation: `true` or `false`'
                          type: boolean
                        isp:
                          description: Only actions coming from this internet service
                            provider are included in the metric calculation.
                          type: string
                        manufacturer:
                          description: Only actions coming from devices of this manufacturer
                            are included in the metric calculation.
                          type: string
                        networkTechnology:
                          description: Filter by network technology
                          type: string
                        orientation:
                          description: 'Only actions coming from devices with this
                            display orientation are included in the metric calculation.
                            Possible values: [ LANDSCAPE, PORTRAIT, UNKNOWN ]'
                          type: string
                        osFamily:
                          description: Only actions coming from this OS family are
                            included in the metric calculation.
                          type: string
                        osVersion:
                          description: Only actions coming from this OS version are
                            included in the metric calculation.
                          type: string
                        region:
                          description: Only actions of users from this region are
                            included in the metric calculation. Specify geolocation
                            ID here.
                          type: string
                        resolution:
                          description: 'Only actions coming from devices with this
                            display resolution are included in the metric calculation.
                            Possible values: [ CGA, DCI2K, DCI4K, DVGA, FHD, FWVGA,
                            FWXGA, GHDPlus, HD, HQVGA, HQVGA2, HSXGA, HUXGA, HVGA,
                            HXGA, NTSC, PAL, QHD, QQVGA, QSXGA, QUXGA, QVGA, QWXGA,
                            QXGA, SVGA, SXGA, SXGAMinus, SXGAPlus, UGA, UHD16K, UHD4K,
                            UHD8K, UHDPlus, UNKNOWN, UWQHD, UXGA, VGA, WHSXGA, WHUXGA,
                            WHXGA, WQSXGA, WQUXGA, WQVGA, WQVGA2, WQVGA3, WQXGA, WQXGA2,
                            WSVGA, WSVGA2, WSXGA, WSXGAPlus, WUXGA, WVGA, WVGA2, WXGA,
                            WXGA2, WXGA3, WXGAPlus, XGA, XGAPLUS, _1280x854, nHD,
                            qHD ]'
                          type: string
                        userActionName:
                          description: Only actions with this name are included in
                            the metric calculation.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.appIdentifier is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.appIdentifier)
                || (has(self.initProvider) && has(self.initProvider.appIdentifier))'
            - message: spec.forProvider.enabled is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enabled)
                || (has(self.initProvider) && has(self.initProvider.enabled))'
            - message: spec.forProvider.metricKey is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metricKey)
                || (has(self.initProvider) && has(self.initProvider.metricKey))'
            - message: spec.forProvider.metricType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metricType)
                || (has(self.initProvider) && has(self.initProvider.metricType))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: MobileMetricStatus defines the observed state of MobileMetric.
            properties:
              atProvider:
                properties:
                  appIdentifier:
                    description: The Dynatrace entity ID of the application to which
                      the metric belongs.
                    type: string
                  description:
                    description: Descriptor of a calculated mobile/custom app metric.
                    type: string
                  dimensions:
                    description: Parameters of a definition of a calculated mobile/custom
                      app metric.
                    items:
                      properties:
                        dimension:
                          description: A dimensions for the metric usage
                          items:
                            properties:
                              dimension:
                                description: The dimension of the metric. Possible
                                  values are `ApdexType`, `Browser`, `ErrorContext`,
                                  `ErrorOrigin`, `ErrorType`, `GeoLocation`, `StringProperty`,
                                  `UserActionType`
                                type: string
                              topX:
                                description: The number of top values to be calculated
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  enabled:
                    description: The metric is enabled (`true`) or disabled (`false`)
                    type: boolean
                  id:
                    type: string
                  metricKey:
                    description: The unique key of the calculated mobile/custom app
                      metric.
                    type: string
                  metricType:
                    description: 'The type of the metric. Possible values: [ REPORTED_ERROR_COUNT,
                      USER_ACTION_DURATION, WEB_REQUEST_COUNT, WEB_REQUEST_ERROR_COUNT
                      ]'
                    type: string
                  name:
                    description: The displayed name of the metric.
                    type: string
                  userActionFilter:
                    description: Parameters of a definition of a calculated mobile/custom
                      app metric.
                    items:
                      properties:
                        actionDurationFromMilliseconds:
                          description: Only actions with a duration more than or equal
                            to this value (in milliseconds) are included in the metric
                            calculation.
                          type: number
                        actionDurationToMilliseconds:
                          description: Only actions with a duration less than or equal
                            to this value (in milliseconds) are included in the metric
                            calculation.
                          type: number
                        apdex:
                          description: 'Only actions with the specified Apdex score
                            are included in the metric calculation. Possible values:
                            [ Frustrated, Satisfied, Tolerating, Unknown ]'
                          type: string
                        appVersion:
                          description: Only actions coming from this app version are
                            included in the metric calculation.
                          type: string
                        carrier:
                          description: Only actions coming from this carrier type
                            are included in the metric calculation.
                          type: string
                        city:
                          description: Only actions of users from this city are included
                            in the metric calculation. Specify geolocation ID here.
                          type: string
                        connectionType:
                          description: 'Only actions coming from this connection type
                            are included in the metric calculation. Possible values:
                            [ LAN, MOBILE, OFFLINE, UNKNOWN, WIFI ]'
                          type: string
                        continent:
                          description: Only actions of users from this continent are
                            included in the metric calculation. Specify geolocation
                            ID here.
                          type: string
                        country:
                          description: Only actions of users from this country are
                            included in the metric calculation. Specify geolocation
                            ID here.
                          type: string
                        device:
                          description: Only actions coming from this app version are
                            included in the metric calculation.
                          type: string
                        hasHttpError:
                          description: 'The request error status of the actions to
                            be included in the metric calculation: `true` or `false`'
                          type: boolean
                        hasReportedError:
                          description: 'The error status of the actions to be included
                            in the metric calculation: `true` or `false`'
                          type: boolean
                        isp:
                          description: Only actions coming from this internet service
                            provider are included in the metric calculation.
                          type: string
                        manufacturer:
                          description: Only actions coming from devices of this manufacturer
                            are included in the metric calculation.
                          type: string
                        networkTechnology:
                          description: Filter by network technology
                          type: string
                        orientation:
                          description: 'Only actions coming from devices with this
                            display orientation are included in the metric calculation.
                            Possible values: [ LANDSCAPE, PORTRAIT, UNKNOWN ]'
                          type: string
                        osFamily:
                          description: Only actions coming from this OS family are
                            included in the metric calculation.
                          type: string
                        osVersion:
                          description: Only actions coming from this OS version are
                            included in the metric calculation.
                          type: string
                        region:
                          description: Only actions of users from this region are
                            included in the metric calculation. Specify geolocation
                            ID here.
                          type: string
                        resolution:
                          description: 'Only actions coming from devices with this
                            display resolution are included in the metric calculation.
                            Possible values: [ CGA, DCI2K, DCI4K, DVGA, FHD, FWVGA,
                            FWXGA, GHDPlus, HD, HQVGA, HQVGA2, HSXGA, HUXGA, HVGA,
                            HXGA, NTSC, PAL, QHD, QQVGA, QSXGA, QUXGA, QVGA, QWXGA,
                            QXGA, SVGA, SXGA, SXGAMinus, SXGAPlus, UGA, UHD16K, UHD4K,
                            UHD8K, UHDPlus, UNKNOWN, UWQHD, UXGA, VGA, WHSXGA, WHUXGA,
                            WHXGA, WQSXGA, WQUXGA, WQVGA, WQVGA2, WQVGA3, WQXGA, WQXGA2,
                            WSVGA, WSVGA2, WSXGA, WSXGAPlus, WUXGA, WVGA, WVGA2, WXGA,
                            WXGA2, WXGA3, WXGAPlus, XGA, XGAPLUS, _1280x854, nHD,
                            qHD ]'
                          type: string
                        userActionName:
                          description: Only actions with this name are included in
                            the metric calculation.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
