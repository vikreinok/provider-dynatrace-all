---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: errorrules.application.dynatrace.m.crossplane.io
spec:
  group: application.dynatrace.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: ErrorRules
    listKind: ErrorRulesList
    plural: errorrules
    singular: errorrules
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ErrorRules is the Schema for the ErrorRuless API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ErrorRulesSpec defines the desired state of ErrorRules
            properties:
              forProvider:
                properties:
                  customErrors:
                    description: |-
                      (Field has overlap with `dynatrace_web_app_custom_errors`) An ordered list of HTTP errors.

                      Rules are evaluated from top to bottom; the first matching rule applies
                    items:
                      properties:
                        rule:
                          description: Configuration of the custom error in the web
                            application
                          items:
                            properties:
                              capture:
                                description: Capture (`true`) or ignore (`false`)
                                  the error
                                type: boolean
                              customAlerting:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Davis AI [problem detection and analysis](https://dt-url.net/a963kd2)
                                type: boolean
                              impactApdex:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Apdex calculation
                                type: boolean
                              keyMatcher:
                                description: The matching operation for the **keyPattern**.
                                  Possible values are `BEGINS_WITH`, `CONTAINS`, `ENDS_WITH`
                                  and `EQUALS`
                                type: string
                              keyPattern:
                                description: The key of the error to look for
                                type: string
                              valueMatcher:
                                description: The matching operation for the **valuePattern**.
                                  Possible values are `BEGINS_WITH`, `CONTAINS`, `ENDS_WITH`
                                  and `EQUALS`.
                                type: string
                              valuePattern:
                                description: The value of the error to look for
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  httpErrors:
                    description: |-
                      (Field has overlap with `dynatrace_web_app_request_errors`) An ordered list of HTTP errors.

                      Rules are evaluated from top to bottom; the first matching rule applies
                    items:
                      properties:
                        rule:
                          description: Configuration of the HTTP error in the web
                            application
                          items:
                            properties:
                              capture:
                                description: Capture (`true`) or ignore (`false`)
                                  the error
                                type: boolean
                              considerBlockedRequests:
                                description: If `true`, match by errors that have
                                  CSP Rule violations
                                type: boolean
                              considerForAi:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Davis AI [problem detection and analysis](https://dt-url.net/a963kd2)
                                type: boolean
                              considerUnknownErrorCode:
                                description: If `true`, match by errors that have
                                  unknown HTTP status code
                                type: boolean
                              errorCodes:
                                description: |-
                                  The HTTP status code or status code range to match by.

                                  This field is required if **considerUnknownErrorCode** AND **considerBlockedRequests** are both set to `false`
                                type: string
                              filter:
                                description: The matching rule for the URL. Popssible
                                  values are `BEGINS_WITH`, `CONTAINS`, `ENDS_WITH`
                                  and `EQUALS`.
                                type: string
                              filterByUrl:
                                description: If `true`, filter errors by URL
                                type: boolean
                              impactApdex:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Apdex calculation
                                type: boolean
                              url:
                                description: The URL to look for
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  ignoreCustomErrorsApdex:
                    description: (Field has overlap with `dynatrace_web_app_custom_errors`)
                      Exclude (`true`) or include (`false`) custom errors listed in
                      **customErrorRules** in Apdex calculation
                    type: boolean
                  ignoreHttpErrorsApdex:
                    description: (Field has overlap with `dynatrace_web_app_request_errors`)
                      Exclude (`true`) or include (`false`) HTTP errors listed in
                      **httpErrorRules** in Apdex calculation
                    type: boolean
                  ignoreJsErrorsApdex:
                    description: Exclude (`true`) or include (`false`) JavaScript
                      errors in Apdex calculation
                    type: boolean
                  webApplicationId:
                    description: The EntityID of the the WebApplication
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  customErrors:
                    description: |-
                      (Field has overlap with `dynatrace_web_app_custom_errors`) An ordered list of HTTP errors.

                      Rules are evaluated from top to bottom; the first matching rule applies
                    items:
                      properties:
                        rule:
                          description: Configuration of the custom error in the web
                            application
                          items:
                            properties:
                              capture:
                                description: Capture (`true`) or ignore (`false`)
                                  the error
                                type: boolean
                              customAlerting:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Davis AI [problem detection and analysis](https://dt-url.net/a963kd2)
                                type: boolean
                              impactApdex:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Apdex calculation
                                type: boolean
                              keyMatcher:
                                description: The matching operation for the **keyPattern**.
                                  Possible values are `BEGINS_WITH`, `CONTAINS`, `ENDS_WITH`
                                  and `EQUALS`
                                type: string
                              keyPattern:
                                description: The key of the error to look for
                                type: string
                              valueMatcher:
                                description: The matching operation for the **valuePattern**.
                                  Possible values are `BEGINS_WITH`, `CONTAINS`, `ENDS_WITH`
                                  and `EQUALS`.
                                type: string
                              valuePattern:
                                description: The value of the error to look for
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  httpErrors:
                    description: |-
                      (Field has overlap with `dynatrace_web_app_request_errors`) An ordered list of HTTP errors.

                      Rules are evaluated from top to bottom; the first matching rule applies
                    items:
                      properties:
                        rule:
                          description: Configuration of the HTTP error in the web
                            application
                          items:
                            properties:
                              capture:
                                description: Capture (`true`) or ignore (`false`)
                                  the error
                                type: boolean
                              considerBlockedRequests:
                                description: If `true`, match by errors that have
                                  CSP Rule violations
                                type: boolean
                              considerForAi:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Davis AI [problem detection and analysis](https://dt-url.net/a963kd2)
                                type: boolean
                              considerUnknownErrorCode:
                                description: If `true`, match by errors that have
                                  unknown HTTP status code
                                type: boolean
                              errorCodes:
                                description: |-
                                  The HTTP status code or status code range to match by.

                                  This field is required if **considerUnknownErrorCode** AND **considerBlockedRequests** are both set to `false`
                                type: string
                              filter:
                                description: The matching rule for the URL. Popssible
                                  values are `BEGINS_WITH`, `CONTAINS`, `ENDS_WITH`
                                  and `EQUALS`.
                                type: string
                              filterByUrl:
                                description: If `true`, filter errors by URL
                                type: boolean
                              impactApdex:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Apdex calculation
                                type: boolean
                              url:
                                description: The URL to look for
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  ignoreCustomErrorsApdex:
                    description: (Field has overlap with `dynatrace_web_app_custom_errors`)
                      Exclude (`true`) or include (`false`) custom errors listed in
                      **customErrorRules** in Apdex calculation
                    type: boolean
                  ignoreHttpErrorsApdex:
                    description: (Field has overlap with `dynatrace_web_app_request_errors`)
                      Exclude (`true`) or include (`false`) HTTP errors listed in
                      **httpErrorRules** in Apdex calculation
                    type: boolean
                  ignoreJsErrorsApdex:
                    description: Exclude (`true`) or include (`false`) JavaScript
                      errors in Apdex calculation
                    type: boolean
                  webApplicationId:
                    description: The EntityID of the the WebApplication
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ErrorRulesStatus defines the observed state of ErrorRules.
            properties:
              atProvider:
                properties:
                  customErrors:
                    description: |-
                      (Field has overlap with `dynatrace_web_app_custom_errors`) An ordered list of HTTP errors.

                      Rules are evaluated from top to bottom; the first matching rule applies
                    items:
                      properties:
                        rule:
                          description: Configuration of the custom error in the web
                            application
                          items:
                            properties:
                              capture:
                                description: Capture (`true`) or ignore (`false`)
                                  the error
                                type: boolean
                              customAlerting:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Davis AI [problem detection and analysis](https://dt-url.net/a963kd2)
                                type: boolean
                              impactApdex:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Apdex calculation
                                type: boolean
                              keyMatcher:
                                description: The matching operation for the **keyPattern**.
                                  Possible values are `BEGINS_WITH`, `CONTAINS`, `ENDS_WITH`
                                  and `EQUALS`
                                type: string
                              keyPattern:
                                description: The key of the error to look for
                                type: string
                              valueMatcher:
                                description: The matching operation for the **valuePattern**.
                                  Possible values are `BEGINS_WITH`, `CONTAINS`, `ENDS_WITH`
                                  and `EQUALS`.
                                type: string
                              valuePattern:
                                description: The value of the error to look for
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  httpErrors:
                    description: |-
                      (Field has overlap with `dynatrace_web_app_request_errors`) An ordered list of HTTP errors.

                      Rules are evaluated from top to bottom; the first matching rule applies
                    items:
                      properties:
                        rule:
                          description: Configuration of the HTTP error in the web
                            application
                          items:
                            properties:
                              capture:
                                description: Capture (`true`) or ignore (`false`)
                                  the error
                                type: boolean
                              considerBlockedRequests:
                                description: If `true`, match by errors that have
                                  CSP Rule violations
                                type: boolean
                              considerForAi:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Davis AI [problem detection and analysis](https://dt-url.net/a963kd2)
                                type: boolean
                              considerUnknownErrorCode:
                                description: If `true`, match by errors that have
                                  unknown HTTP status code
                                type: boolean
                              errorCodes:
                                description: |-
                                  The HTTP status code or status code range to match by.

                                  This field is required if **considerUnknownErrorCode** AND **considerBlockedRequests** are both set to `false`
                                type: string
                              filter:
                                description: The matching rule for the URL. Popssible
                                  values are `BEGINS_WITH`, `CONTAINS`, `ENDS_WITH`
                                  and `EQUALS`.
                                type: string
                              filterByUrl:
                                description: If `true`, filter errors by URL
                                type: boolean
                              impactApdex:
                                description: Include (`true`) or exclude (`false`)
                                  the error in Apdex calculation
                                type: boolean
                              url:
                                description: The URL to look for
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  id:
                    type: string
                  ignoreCustomErrorsApdex:
                    description: (Field has overlap with `dynatrace_web_app_custom_errors`)
                      Exclude (`true`) or include (`false`) custom errors listed in
                      **customErrorRules** in Apdex calculation
                    type: boolean
                  ignoreHttpErrorsApdex:
                    description: (Field has overlap with `dynatrace_web_app_request_errors`)
                      Exclude (`true`) or include (`false`) HTTP errors listed in
                      **httpErrorRules** in Apdex calculation
                    type: boolean
                  ignoreJsErrorsApdex:
                    description: Exclude (`true`) or include (`false`) JavaScript
                      errors in Apdex calculation
                    type: boolean
                  webApplicationId:
                    description: The EntityID of the the WebApplication
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
