---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: workloadanomalies.k8s.dynatrace.crossplane.io
spec:
  group: k8s.dynatrace.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: WorkloadAnomalies
    listKind: WorkloadAnomaliesList
    plural: workloadanomalies
    singular: workloadanomalies
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkloadAnomalies is the Schema for the WorkloadAnomaliess API.
          The resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkloadAnomaliesSpec defines the desired state of WorkloadAnomalies
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  containerRestarts:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  per minute, for any
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  there is at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  deploymentStuck:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  workload stops progressing for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  highCpuThrottling:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  of CPU usage for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  amount of CPU throttling is above
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  highCpuUsage:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  of defined CPU limits for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  amount of utilized workload CPU is above
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  highMemoryUsage:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  of defined memory limits for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  amount of utilized workload memory is above
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  jobFailureEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  notAllPodsReady:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  some workload pods are not ready for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  oomKills:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  pendingPods:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  stuck in pending state for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  there is at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podBackoffEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podEvictionEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podPreemptionEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podStuckInTerminating:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  pod termination stops progressing for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  scope:
                    description: |-
                      (String) The scope of this setting (CLOUD_APPLICATION_NAMESPACE, KUBERNETES_CLUSTER). Omit this property if you want to cover the whole environment.
                      The scope of this setting (CLOUD_APPLICATION_NAMESPACE, KUBERNETES_CLUSTER). Omit this property if you want to cover the whole environment.
                    type: string
                  workloadWithoutReadyPods:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  workload has no ready pods for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  containerRestarts:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  per minute, for any
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  there is at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  deploymentStuck:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  workload stops progressing for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  highCpuThrottling:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  of CPU usage for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  amount of CPU throttling is above
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  highCpuUsage:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  of defined CPU limits for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  amount of utilized workload CPU is above
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  highMemoryUsage:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  of defined memory limits for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  amount of utilized workload memory is above
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  jobFailureEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  notAllPodsReady:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  some workload pods are not ready for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  oomKills:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  pendingPods:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  stuck in pending state for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  there is at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podBackoffEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podEvictionEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podPreemptionEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podStuckInTerminating:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  pod termination stops progressing for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  scope:
                    description: |-
                      (String) The scope of this setting (CLOUD_APPLICATION_NAMESPACE, KUBERNETES_CLUSTER). Omit this property if you want to cover the whole environment.
                      The scope of this setting (CLOUD_APPLICATION_NAMESPACE, KUBERNETES_CLUSTER). Omit this property if you want to cover the whole environment.
                    type: string
                  workloadWithoutReadyPods:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  workload has no ready pods for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.containerRestarts is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.containerRestarts)
                || (has(self.initProvider) && has(self.initProvider.containerRestarts))'
            - message: spec.forProvider.deploymentStuck is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.deploymentStuck)
                || (has(self.initProvider) && has(self.initProvider.deploymentStuck))'
            - message: spec.forProvider.highCpuThrottling is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.highCpuThrottling)
                || (has(self.initProvider) && has(self.initProvider.highCpuThrottling))'
            - message: spec.forProvider.highCpuUsage is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.highCpuUsage)
                || (has(self.initProvider) && has(self.initProvider.highCpuUsage))'
            - message: spec.forProvider.highMemoryUsage is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.highMemoryUsage)
                || (has(self.initProvider) && has(self.initProvider.highMemoryUsage))'
            - message: spec.forProvider.jobFailureEvents is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.jobFailureEvents)
                || (has(self.initProvider) && has(self.initProvider.jobFailureEvents))'
            - message: spec.forProvider.notAllPodsReady is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.notAllPodsReady)
                || (has(self.initProvider) && has(self.initProvider.notAllPodsReady))'
            - message: spec.forProvider.oomKills is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.oomKills)
                || (has(self.initProvider) && has(self.initProvider.oomKills))'
            - message: spec.forProvider.pendingPods is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.pendingPods)
                || (has(self.initProvider) && has(self.initProvider.pendingPods))'
            - message: spec.forProvider.podBackoffEvents is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.podBackoffEvents)
                || (has(self.initProvider) && has(self.initProvider.podBackoffEvents))'
            - message: spec.forProvider.podEvictionEvents is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.podEvictionEvents)
                || (has(self.initProvider) && has(self.initProvider.podEvictionEvents))'
            - message: spec.forProvider.podPreemptionEvents is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.podPreemptionEvents)
                || (has(self.initProvider) && has(self.initProvider.podPreemptionEvents))'
            - message: spec.forProvider.podStuckInTerminating is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.podStuckInTerminating)
                || (has(self.initProvider) && has(self.initProvider.podStuckInTerminating))'
            - message: spec.forProvider.workloadWithoutReadyPods is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.workloadWithoutReadyPods)
                || (has(self.initProvider) && has(self.initProvider.workloadWithoutReadyPods))'
          status:
            description: WorkloadAnomaliesStatus defines the observed state of WorkloadAnomalies.
            properties:
              atProvider:
                properties:
                  containerRestarts:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  per minute, for any
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  there is at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  deploymentStuck:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  workload stops progressing for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  highCpuThrottling:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  of CPU usage for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  amount of CPU throttling is above
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  highCpuUsage:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  of defined CPU limits for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  amount of utilized workload CPU is above
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  highMemoryUsage:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  of defined memory limits for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  amount of utilized workload memory is above
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  jobFailureEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  notAllPodsReady:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  some workload pods are not ready for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  oomKills:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  pendingPods:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  stuck in pending state for at least
                                type: number
                              threshold:
                                description: |-
                                  (Number) there is at least
                                  there is at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podBackoffEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podEvictionEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podPreemptionEvents:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  podStuckInTerminating:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  pod termination stops progressing for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                  scope:
                    description: |-
                      (String) The scope of this setting (CLOUD_APPLICATION_NAMESPACE, KUBERNETES_CLUSTER). Omit this property if you want to cover the whole environment.
                      The scope of this setting (CLOUD_APPLICATION_NAMESPACE, KUBERNETES_CLUSTER). Omit this property if you want to cover the whole environment.
                    type: string
                  workloadWithoutReadyPods:
                    description: |-
                      (Block List, Min: 1, Max: 1) no documentation available (see below for nested schema)
                      no documentation available
                    items:
                      properties:
                        configuration:
                          description: |-
                            (Block List, Max: 1) Alert if (see below for nested schema)
                            Alert if
                          items:
                            properties:
                              observationPeriodInMinutes:
                                description: |-
                                  (Number) within the last
                                  within the last
                                type: number
                              samplePeriodInMinutes:
                                description: |-
                                  (Number) per minute, for any
                                  workload has no ready pods for at least
                                type: number
                            type: object
                          type: array
                        enabled:
                          description: |-
                            (Boolean) This setting is enabled (true) or disabled (false)
                            This setting is enabled (`true`) or disabled (`false`)
                          type: boolean
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
