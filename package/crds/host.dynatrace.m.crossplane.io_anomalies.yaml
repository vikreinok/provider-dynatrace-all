---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: anomalies.host.dynatrace.m.crossplane.io
spec:
  group: host.dynatrace.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - dynatrace
    kind: Anomalies
    listKind: AnomaliesList
    plural: anomalies
    singular: anomalies
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Anomalies is the Schema for the Anomaliess API. The resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AnomaliesSpec defines the desired state of Anomalies
            properties:
              forProvider:
                properties:
                  connections:
                    description: |-
                      (Block List, Max: 1) Configuration of lost connection detection (see below for nested schema)
                      Configuration of lost connection detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        enabledOnGracefulShutdowns:
                          description: |-
                            (Boolean) Alert (true) on graceful host shutdowns
                            Alert (`true`) on graceful host shutdowns
                          type: boolean
                      type: object
                    type: array
                  cpu:
                    description: |-
                      (Block List, Max: 1) Configuration of high CPU saturation detection (see below for nested schema)
                      Configuration of high CPU saturation detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        thresholds:
                          description: |-
                            (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                            Custom thresholds for high CPU saturation. If not set then the automatic mode is used
                          items:
                            properties:
                              saturation:
                                description: |-
                                  (Number) Alert if CPU usage is higher than X% in 3 out of 5 samples
                                  Alert if CPU usage is higher than *X*% in 3 out of 5 samples
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  disks:
                    description: |-
                      (Block List, Max: 1) Configuration of disk related anomalies (see below for nested schema)
                      Configuration of disk related anomalies
                    items:
                      properties:
                        inodes:
                          description: |-
                            (Block List, Max: 1) Configuration of low disk inodes number detection (see below for nested schema)
                            Configuration of low disk inodes number detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for low disk inodes number. If not set, automatic mode is used
                                items:
                                  properties:
                                    percentage:
                                      description: |-
                                        (Number) Alert if percentage of available inodes is lower than X% in 3 out of 5 samples
                                        Alert if percentage of available inodes is lower than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        space:
                          description: |-
                            (Block List, Max: 1) Configuration of low disk space detection (see below for nested schema)
                            Configuration of low disk space detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for low disk space. If not set, automatic mode is used
                                items:
                                  properties:
                                    percentage:
                                      description: |-
                                        (Number) Alert if percentage of available inodes is lower than X% in 3 out of 5 samples
                                        Alert if free disk space is lower than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        speed:
                          description: |-
                            (Block List, Max: 1) Configuration of slow running disks detection (see below for nested schema)
                            Configuration of slow running disks detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for slow running disks. If not set, the automatic mode is used
                                items:
                                  properties:
                                    writeAndReadTime:
                                      description: |-
                                        (Number) Alert if disk read/write time is higher than X milliseconds in 3 out of 5 samples
                                        Alert if disk read/write time is higher than *X* milliseconds in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  gc:
                    description: |-
                      (Block List, Max: 1) Configuration of high Garbage Collector activity detection (see below for nested schema)
                      Configuration of high Garbage Collector activity detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        thresholds:
                          description: |-
                            (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                            Custom thresholds for high GC activity. If not set, automatic mode is used.   Meeting **any** of these conditions triggers an alert
                          items:
                            properties:
                              suspensionPercentage:
                                description: |-
                                  (Number) GC suspension is higher than X% in 3 out of 5 samples
                                  GC suspension is higher than *X*% in 3 out of 5 samples
                                type: number
                              timePercentage:
                                description: |-
                                  (Number) GC time is higher than X% in 3 out of 5 samples
                                  GC time is higher than *X*% in 3 out of 5 samples
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  java:
                    description: |-
                      (Block List, Max: 1) Configuration of Java related anomalies (see below for nested schema)
                      Configuration of Java related anomalies
                    items:
                      properties:
                        outOfMemory:
                          description: |-
                            (Block List, Max: 1) Configuration of Java out of memory problems detection (see below for nested schema)
                            Configuration of Java out of memory problems detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for Java out of memory. If not set, automatic mode is used
                                items:
                                  properties:
                                    exceptionCount:
                                      description: |-
                                        (Number) Alert if the number of Java out of memory exceptions is X per minute or higher
                                        Alert if the number of Java out of memory exceptions is *X* per minute or higher
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        outOfThreads:
                          description: |-
                            (Block List, Max: 1) Configuration of Java out of threads problems detection (see below for nested schema)
                            Configuration of Java out of threads problems detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for Java out of threads detection. If not set, automatic mode is used
                                items:
                                  properties:
                                    exceptionCount:
                                      description: |-
                                        (Number) Alert if the number of Java out of memory exceptions is X per minute or higher
                                        Alert if the number of Java out of threads exceptions is *X* per minute or higher
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  memory:
                    description: |-
                      (Block List, Max: 1) Configuration of high memory usage detection (see below for nested schema)
                      Configuration of high memory usage detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        thresholds:
                          description: |-
                            (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                            Custom thresholds for high memory usage. If not set then the automatic mode is used. **Both** conditions must be met to trigger an alert
                          items:
                            properties:
                              linux:
                                description: |-
                                  (Block List, Min: 1, Max: 1) Custom thresholds for Linux (see below for nested schema)
                                  Custom thresholds for Linux
                                items:
                                  properties:
                                    pageFaults:
                                      description: |-
                                        (Number) Memory page fault rate is higher than X faults per second
                                        Memory page fault rate is higher than *X* faults per second
                                      type: number
                                    usage:
                                      description: |-
                                        (Number) Memory usage is higher than X%
                                        Memory usage is higher than *X*%
                                      type: number
                                  type: object
                                type: array
                              windows:
                                description: |-
                                  (Block List, Min: 1, Max: 1) Custom thresholds for Windows (see below for nested schema)
                                  Custom thresholds for Windows
                                items:
                                  properties:
                                    pageFaults:
                                      description: |-
                                        (Number) Memory page fault rate is higher than X faults per second
                                        Memory page fault rate is higher than *X* faults per second
                                      type: number
                                    usage:
                                      description: |-
                                        (Number) Memory usage is higher than X%
                                        Memory usage is higher than *X*%
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  network:
                    description: |-
                      (Block List, Max: 1) Configuration of network related anomalies (see below for nested schema)
                      Configuration of network related anomalies
                    items:
                      properties:
                        connectivity:
                          description: |-
                            (Block List, Max: 1) Configuration of TCP connectivity problems detection (see below for nested schema)
                            Configuration of TCP connectivity problems detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for TCP connection problems. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    failedConnections:
                                      description: |-
                                        (Number) Number of failed connections is higher than X connections per minute in 3 out of 5 samples
                                        Number of failed connections is higher than *X* connections per minute in 3 out of 5 samples
                                      type: number
                                    newConnectionFailures:
                                      description: |-
                                        (Number) Percentage of new connection failures is higher than X% in 3 out of 5 samples
                                        Percentage of new connection failures is higher than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        droppedPackets:
                          description: |-
                            (Block List, Max: 1) Configuration of high number of dropped packets detection (see below for nested schema)
                            Configuration of high number of dropped packets detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for dropped packets. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    droppedPackets:
                                      description: |-
                                        (Block List, Max: 1) Configuration of high number of dropped packets detection (see below for nested schema)
                                        Receive/transmit dropped packet percentage is higher than *X*% in 3 out of 5 samples
                                      type: number
                                    totalPacketsRate:
                                      description: |-
                                        (Number) Total receive/transmit packets rate is higher than X packets per second in 3 out of 5 samples
                                        Total receive/transmit packets rate is higher than *X* packets per second in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        errors:
                          description: |-
                            (Block List, Max: 1) Configuration of high number of network errors detection (see below for nested schema)
                            Configuration of high number of network errors detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for network errors. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    errorsPercentage:
                                      description: |-
                                        (Number) Receive/transmit error packet percentage is higher than X% in 3 out of 5 samples
                                        Receive/transmit error packet percentage is higher than *X*% in 3 out of 5 samples
                                      type: number
                                    totalPacketsRate:
                                      description: |-
                                        (Number) Total receive/transmit packets rate is higher than X packets per second in 3 out of 5 samples
                                        Total receive/transmit packets rate is higher than *X* packets per second in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        retransmission:
                          description: |-
                            (Block List, Max: 1) Configuration of high retransmission rate detection (see below for nested schema)
                            Configuration of high retransmission rate detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for high retransmission rate. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    retransmissionRate:
                                      description: |-
                                        (Number) Retransmission rate is higher than X% in 3 out of 5 samples
                                        Retransmission rate is higher than *X*% in 3 out of 5 samples
                                      type: number
                                    retransmittedPackets:
                                      description: |-
                                        (Number) Number of retransmitted packets is higher than X packets per minute in 3 out of 5 samples
                                        Number of retransmitted packets is higher than *X* packets per minute in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        utilization:
                          description: |-
                            (Block List, Max: 1) Configuration of high network utilization detection (see below for nested schema)
                            Configuration of high network utilization detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for high network utilization. If not set, automatic mode is used
                                items:
                                  properties:
                                    utilization:
                                      description: |-
                                        (Block List, Max: 1) Configuration of high network utilization detection (see below for nested schema)
                                        Alert if sent/received traffic utilization is higher than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  connections:
                    description: |-
                      (Block List, Max: 1) Configuration of lost connection detection (see below for nested schema)
                      Configuration of lost connection detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        enabledOnGracefulShutdowns:
                          description: |-
                            (Boolean) Alert (true) on graceful host shutdowns
                            Alert (`true`) on graceful host shutdowns
                          type: boolean
                      type: object
                    type: array
                  cpu:
                    description: |-
                      (Block List, Max: 1) Configuration of high CPU saturation detection (see below for nested schema)
                      Configuration of high CPU saturation detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        thresholds:
                          description: |-
                            (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                            Custom thresholds for high CPU saturation. If not set then the automatic mode is used
                          items:
                            properties:
                              saturation:
                                description: |-
                                  (Number) Alert if CPU usage is higher than X% in 3 out of 5 samples
                                  Alert if CPU usage is higher than *X*% in 3 out of 5 samples
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  disks:
                    description: |-
                      (Block List, Max: 1) Configuration of disk related anomalies (see below for nested schema)
                      Configuration of disk related anomalies
                    items:
                      properties:
                        inodes:
                          description: |-
                            (Block List, Max: 1) Configuration of low disk inodes number detection (see below for nested schema)
                            Configuration of low disk inodes number detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for low disk inodes number. If not set, automatic mode is used
                                items:
                                  properties:
                                    percentage:
                                      description: |-
                                        (Number) Alert if percentage of available inodes is lower than X% in 3 out of 5 samples
                                        Alert if percentage of available inodes is lower than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        space:
                          description: |-
                            (Block List, Max: 1) Configuration of low disk space detection (see below for nested schema)
                            Configuration of low disk space detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for low disk space. If not set, automatic mode is used
                                items:
                                  properties:
                                    percentage:
                                      description: |-
                                        (Number) Alert if percentage of available inodes is lower than X% in 3 out of 5 samples
                                        Alert if free disk space is lower than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        speed:
                          description: |-
                            (Block List, Max: 1) Configuration of slow running disks detection (see below for nested schema)
                            Configuration of slow running disks detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for slow running disks. If not set, the automatic mode is used
                                items:
                                  properties:
                                    writeAndReadTime:
                                      description: |-
                                        (Number) Alert if disk read/write time is higher than X milliseconds in 3 out of 5 samples
                                        Alert if disk read/write time is higher than *X* milliseconds in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  gc:
                    description: |-
                      (Block List, Max: 1) Configuration of high Garbage Collector activity detection (see below for nested schema)
                      Configuration of high Garbage Collector activity detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        thresholds:
                          description: |-
                            (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                            Custom thresholds for high GC activity. If not set, automatic mode is used.   Meeting **any** of these conditions triggers an alert
                          items:
                            properties:
                              suspensionPercentage:
                                description: |-
                                  (Number) GC suspension is higher than X% in 3 out of 5 samples
                                  GC suspension is higher than *X*% in 3 out of 5 samples
                                type: number
                              timePercentage:
                                description: |-
                                  (Number) GC time is higher than X% in 3 out of 5 samples
                                  GC time is higher than *X*% in 3 out of 5 samples
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  java:
                    description: |-
                      (Block List, Max: 1) Configuration of Java related anomalies (see below for nested schema)
                      Configuration of Java related anomalies
                    items:
                      properties:
                        outOfMemory:
                          description: |-
                            (Block List, Max: 1) Configuration of Java out of memory problems detection (see below for nested schema)
                            Configuration of Java out of memory problems detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for Java out of memory. If not set, automatic mode is used
                                items:
                                  properties:
                                    exceptionCount:
                                      description: |-
                                        (Number) Alert if the number of Java out of memory exceptions is X per minute or higher
                                        Alert if the number of Java out of memory exceptions is *X* per minute or higher
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        outOfThreads:
                          description: |-
                            (Block List, Max: 1) Configuration of Java out of threads problems detection (see below for nested schema)
                            Configuration of Java out of threads problems detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for Java out of threads detection. If not set, automatic mode is used
                                items:
                                  properties:
                                    exceptionCount:
                                      description: |-
                                        (Number) Alert if the number of Java out of memory exceptions is X per minute or higher
                                        Alert if the number of Java out of threads exceptions is *X* per minute or higher
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  memory:
                    description: |-
                      (Block List, Max: 1) Configuration of high memory usage detection (see below for nested schema)
                      Configuration of high memory usage detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        thresholds:
                          description: |-
                            (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                            Custom thresholds for high memory usage. If not set then the automatic mode is used. **Both** conditions must be met to trigger an alert
                          items:
                            properties:
                              linux:
                                description: |-
                                  (Block List, Min: 1, Max: 1) Custom thresholds for Linux (see below for nested schema)
                                  Custom thresholds for Linux
                                items:
                                  properties:
                                    pageFaults:
                                      description: |-
                                        (Number) Memory page fault rate is higher than X faults per second
                                        Memory page fault rate is higher than *X* faults per second
                                      type: number
                                    usage:
                                      description: |-
                                        (Number) Memory usage is higher than X%
                                        Memory usage is higher than *X*%
                                      type: number
                                  type: object
                                type: array
                              windows:
                                description: |-
                                  (Block List, Min: 1, Max: 1) Custom thresholds for Windows (see below for nested schema)
                                  Custom thresholds for Windows
                                items:
                                  properties:
                                    pageFaults:
                                      description: |-
                                        (Number) Memory page fault rate is higher than X faults per second
                                        Memory page fault rate is higher than *X* faults per second
                                      type: number
                                    usage:
                                      description: |-
                                        (Number) Memory usage is higher than X%
                                        Memory usage is higher than *X*%
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  network:
                    description: |-
                      (Block List, Max: 1) Configuration of network related anomalies (see below for nested schema)
                      Configuration of network related anomalies
                    items:
                      properties:
                        connectivity:
                          description: |-
                            (Block List, Max: 1) Configuration of TCP connectivity problems detection (see below for nested schema)
                            Configuration of TCP connectivity problems detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for TCP connection problems. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    failedConnections:
                                      description: |-
                                        (Number) Number of failed connections is higher than X connections per minute in 3 out of 5 samples
                                        Number of failed connections is higher than *X* connections per minute in 3 out of 5 samples
                                      type: number
                                    newConnectionFailures:
                                      description: |-
                                        (Number) Percentage of new connection failures is higher than X% in 3 out of 5 samples
                                        Percentage of new connection failures is higher than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        droppedPackets:
                          description: |-
                            (Block List, Max: 1) Configuration of high number of dropped packets detection (see below for nested schema)
                            Configuration of high number of dropped packets detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for dropped packets. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    droppedPackets:
                                      description: |-
                                        (Block List, Max: 1) Configuration of high number of dropped packets detection (see below for nested schema)
                                        Receive/transmit dropped packet percentage is higher than *X*% in 3 out of 5 samples
                                      type: number
                                    totalPacketsRate:
                                      description: |-
                                        (Number) Total receive/transmit packets rate is higher than X packets per second in 3 out of 5 samples
                                        Total receive/transmit packets rate is higher than *X* packets per second in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        errors:
                          description: |-
                            (Block List, Max: 1) Configuration of high number of network errors detection (see below for nested schema)
                            Configuration of high number of network errors detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for network errors. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    errorsPercentage:
                                      description: |-
                                        (Number) Receive/transmit error packet percentage is higher than X% in 3 out of 5 samples
                                        Receive/transmit error packet percentage is higher than *X*% in 3 out of 5 samples
                                      type: number
                                    totalPacketsRate:
                                      description: |-
                                        (Number) Total receive/transmit packets rate is higher than X packets per second in 3 out of 5 samples
                                        Total receive/transmit packets rate is higher than *X* packets per second in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        retransmission:
                          description: |-
                            (Block List, Max: 1) Configuration of high retransmission rate detection (see below for nested schema)
                            Configuration of high retransmission rate detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for high retransmission rate. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    retransmissionRate:
                                      description: |-
                                        (Number) Retransmission rate is higher than X% in 3 out of 5 samples
                                        Retransmission rate is higher than *X*% in 3 out of 5 samples
                                      type: number
                                    retransmittedPackets:
                                      description: |-
                                        (Number) Number of retransmitted packets is higher than X packets per minute in 3 out of 5 samples
                                        Number of retransmitted packets is higher than *X* packets per minute in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        utilization:
                          description: |-
                            (Block List, Max: 1) Configuration of high network utilization detection (see below for nested schema)
                            Configuration of high network utilization detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for high network utilization. If not set, automatic mode is used
                                items:
                                  properties:
                                    utilization:
                                      description: |-
                                        (Block List, Max: 1) Configuration of high network utilization detection (see below for nested schema)
                                        Alert if sent/received traffic utilization is higher than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: AnomaliesStatus defines the observed state of Anomalies.
            properties:
              atProvider:
                properties:
                  connections:
                    description: |-
                      (Block List, Max: 1) Configuration of lost connection detection (see below for nested schema)
                      Configuration of lost connection detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        enabledOnGracefulShutdowns:
                          description: |-
                            (Boolean) Alert (true) on graceful host shutdowns
                            Alert (`true`) on graceful host shutdowns
                          type: boolean
                      type: object
                    type: array
                  cpu:
                    description: |-
                      (Block List, Max: 1) Configuration of high CPU saturation detection (see below for nested schema)
                      Configuration of high CPU saturation detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        thresholds:
                          description: |-
                            (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                            Custom thresholds for high CPU saturation. If not set then the automatic mode is used
                          items:
                            properties:
                              saturation:
                                description: |-
                                  (Number) Alert if CPU usage is higher than X% in 3 out of 5 samples
                                  Alert if CPU usage is higher than *X*% in 3 out of 5 samples
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  disks:
                    description: |-
                      (Block List, Max: 1) Configuration of disk related anomalies (see below for nested schema)
                      Configuration of disk related anomalies
                    items:
                      properties:
                        inodes:
                          description: |-
                            (Block List, Max: 1) Configuration of low disk inodes number detection (see below for nested schema)
                            Configuration of low disk inodes number detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for low disk inodes number. If not set, automatic mode is used
                                items:
                                  properties:
                                    percentage:
                                      description: |-
                                        (Number) Alert if percentage of available inodes is lower than X% in 3 out of 5 samples
                                        Alert if percentage of available inodes is lower than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        space:
                          description: |-
                            (Block List, Max: 1) Configuration of low disk space detection (see below for nested schema)
                            Configuration of low disk space detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for low disk space. If not set, automatic mode is used
                                items:
                                  properties:
                                    percentage:
                                      description: |-
                                        (Number) Alert if percentage of available inodes is lower than X% in 3 out of 5 samples
                                        Alert if free disk space is lower than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        speed:
                          description: |-
                            (Block List, Max: 1) Configuration of slow running disks detection (see below for nested schema)
                            Configuration of slow running disks detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for slow running disks. If not set, the automatic mode is used
                                items:
                                  properties:
                                    writeAndReadTime:
                                      description: |-
                                        (Number) Alert if disk read/write time is higher than X milliseconds in 3 out of 5 samples
                                        Alert if disk read/write time is higher than *X* milliseconds in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  gc:
                    description: |-
                      (Block List, Max: 1) Configuration of high Garbage Collector activity detection (see below for nested schema)
                      Configuration of high Garbage Collector activity detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        thresholds:
                          description: |-
                            (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                            Custom thresholds for high GC activity. If not set, automatic mode is used.   Meeting **any** of these conditions triggers an alert
                          items:
                            properties:
                              suspensionPercentage:
                                description: |-
                                  (Number) GC suspension is higher than X% in 3 out of 5 samples
                                  GC suspension is higher than *X*% in 3 out of 5 samples
                                type: number
                              timePercentage:
                                description: |-
                                  (Number) GC time is higher than X% in 3 out of 5 samples
                                  GC time is higher than *X*% in 3 out of 5 samples
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  java:
                    description: |-
                      (Block List, Max: 1) Configuration of Java related anomalies (see below for nested schema)
                      Configuration of Java related anomalies
                    items:
                      properties:
                        outOfMemory:
                          description: |-
                            (Block List, Max: 1) Configuration of Java out of memory problems detection (see below for nested schema)
                            Configuration of Java out of memory problems detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for Java out of memory. If not set, automatic mode is used
                                items:
                                  properties:
                                    exceptionCount:
                                      description: |-
                                        (Number) Alert if the number of Java out of memory exceptions is X per minute or higher
                                        Alert if the number of Java out of memory exceptions is *X* per minute or higher
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        outOfThreads:
                          description: |-
                            (Block List, Max: 1) Configuration of Java out of threads problems detection (see below for nested schema)
                            Configuration of Java out of threads problems detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for Java out of threads detection. If not set, automatic mode is used
                                items:
                                  properties:
                                    exceptionCount:
                                      description: |-
                                        (Number) Alert if the number of Java out of memory exceptions is X per minute or higher
                                        Alert if the number of Java out of threads exceptions is *X* per minute or higher
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  memory:
                    description: |-
                      (Block List, Max: 1) Configuration of high memory usage detection (see below for nested schema)
                      Configuration of high memory usage detection
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) The detection is enabled (true) or disabled (false)
                            The detection is enabled (`true`) or disabled (`false`)
                          type: boolean
                        thresholds:
                          description: |-
                            (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                            Custom thresholds for high memory usage. If not set then the automatic mode is used. **Both** conditions must be met to trigger an alert
                          items:
                            properties:
                              linux:
                                description: |-
                                  (Block List, Min: 1, Max: 1) Custom thresholds for Linux (see below for nested schema)
                                  Custom thresholds for Linux
                                items:
                                  properties:
                                    pageFaults:
                                      description: |-
                                        (Number) Memory page fault rate is higher than X faults per second
                                        Memory page fault rate is higher than *X* faults per second
                                      type: number
                                    usage:
                                      description: |-
                                        (Number) Memory usage is higher than X%
                                        Memory usage is higher than *X*%
                                      type: number
                                  type: object
                                type: array
                              windows:
                                description: |-
                                  (Block List, Min: 1, Max: 1) Custom thresholds for Windows (see below for nested schema)
                                  Custom thresholds for Windows
                                items:
                                  properties:
                                    pageFaults:
                                      description: |-
                                        (Number) Memory page fault rate is higher than X faults per second
                                        Memory page fault rate is higher than *X* faults per second
                                      type: number
                                    usage:
                                      description: |-
                                        (Number) Memory usage is higher than X%
                                        Memory usage is higher than *X*%
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  network:
                    description: |-
                      (Block List, Max: 1) Configuration of network related anomalies (see below for nested schema)
                      Configuration of network related anomalies
                    items:
                      properties:
                        connectivity:
                          description: |-
                            (Block List, Max: 1) Configuration of TCP connectivity problems detection (see below for nested schema)
                            Configuration of TCP connectivity problems detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for TCP connection problems. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    failedConnections:
                                      description: |-
                                        (Number) Number of failed connections is higher than X connections per minute in 3 out of 5 samples
                                        Number of failed connections is higher than *X* connections per minute in 3 out of 5 samples
                                      type: number
                                    newConnectionFailures:
                                      description: |-
                                        (Number) Percentage of new connection failures is higher than X% in 3 out of 5 samples
                                        Percentage of new connection failures is higher than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        droppedPackets:
                          description: |-
                            (Block List, Max: 1) Configuration of high number of dropped packets detection (see below for nested schema)
                            Configuration of high number of dropped packets detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for dropped packets. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    droppedPackets:
                                      description: |-
                                        (Block List, Max: 1) Configuration of high number of dropped packets detection (see below for nested schema)
                                        Receive/transmit dropped packet percentage is higher than *X*% in 3 out of 5 samples
                                      type: number
                                    totalPacketsRate:
                                      description: |-
                                        (Number) Total receive/transmit packets rate is higher than X packets per second in 3 out of 5 samples
                                        Total receive/transmit packets rate is higher than *X* packets per second in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        errors:
                          description: |-
                            (Block List, Max: 1) Configuration of high number of network errors detection (see below for nested schema)
                            Configuration of high number of network errors detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for network errors. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    errorsPercentage:
                                      description: |-
                                        (Number) Receive/transmit error packet percentage is higher than X% in 3 out of 5 samples
                                        Receive/transmit error packet percentage is higher than *X*% in 3 out of 5 samples
                                      type: number
                                    totalPacketsRate:
                                      description: |-
                                        (Number) Total receive/transmit packets rate is higher than X packets per second in 3 out of 5 samples
                                        Total receive/transmit packets rate is higher than *X* packets per second in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        retransmission:
                          description: |-
                            (Block List, Max: 1) Configuration of high retransmission rate detection (see below for nested schema)
                            Configuration of high retransmission rate detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for high retransmission rate. If not set, automatic mode is used.   **All** of these conditions must be met to trigger an alert
                                items:
                                  properties:
                                    retransmissionRate:
                                      description: |-
                                        (Number) Retransmission rate is higher than X% in 3 out of 5 samples
                                        Retransmission rate is higher than *X*% in 3 out of 5 samples
                                      type: number
                                    retransmittedPackets:
                                      description: |-
                                        (Number) Number of retransmitted packets is higher than X packets per minute in 3 out of 5 samples
                                        Number of retransmitted packets is higher than *X* packets per minute in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                        utilization:
                          description: |-
                            (Block List, Max: 1) Configuration of high network utilization detection (see below for nested schema)
                            Configuration of high network utilization detection
                          items:
                            properties:
                              enabled:
                                description: |-
                                  (Boolean) The detection is enabled (true) or disabled (false)
                                  The detection is enabled (`true`) or disabled (`false`)
                                type: boolean
                              thresholds:
                                description: |-
                                  (Block List, Max: 1) Custom thresholds for high CPU saturation. If not set then the automatic mode is used (see below for nested schema)
                                  Custom thresholds for high network utilization. If not set, automatic mode is used
                                items:
                                  properties:
                                    utilization:
                                      description: |-
                                        (Block List, Max: 1) Configuration of high network utilization detection (see below for nested schema)
                                        Alert if sent/received traffic utilization is higher than *X*% in 3 out of 5 samples
                                      type: number
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
