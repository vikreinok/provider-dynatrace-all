// SPDX-FileCopyrightText: 2024 The Crossplane Authors <https://crossplane.io>
//
// SPDX-License-Identifier: Apache-2.0

// Code generated by upjet. DO NOT EDIT.

package v1alpha1

import (
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	"k8s.io/apimachinery/pkg/runtime/schema"

	v1 "github.com/crossplane/crossplane-runtime/v2/apis/common/v1"
	v2 "github.com/crossplane/crossplane-runtime/v2/apis/common/v2"
)

type MetricsInitParameters struct {

	// (Boolean) When enabled, you can ingest the le dimension, representing explicit histogram buckets.
	// Enable this if you are using OpenTelemetry histograms or Prometheus histogram metrics.
	// When disabled, only your histograms' sum and count metrics will be ingested.
	// When enabled, you can ingest the `le` dimension, representing explicit histogram buckets.\
	// Enable this if you are using OpenTelemetry histograms or Prometheus histogram metrics.\
	// When disabled, only your histograms' sum and count metrics will be ingested.
	EnableHistogramBucketIngest *bool `json:"enableHistogramBucketIngest,omitempty" tf:"enable_histogram_bucket_ingest,omitempty"`
}

type MetricsObservation struct {

	// (Boolean) When enabled, you can ingest the le dimension, representing explicit histogram buckets.
	// Enable this if you are using OpenTelemetry histograms or Prometheus histogram metrics.
	// When disabled, only your histograms' sum and count metrics will be ingested.
	// When enabled, you can ingest the `le` dimension, representing explicit histogram buckets.\
	// Enable this if you are using OpenTelemetry histograms or Prometheus histogram metrics.\
	// When disabled, only your histograms' sum and count metrics will be ingested.
	EnableHistogramBucketIngest *bool `json:"enableHistogramBucketIngest,omitempty" tf:"enable_histogram_bucket_ingest,omitempty"`

	// (String) The ID of this resource.
	ID *string `json:"id,omitempty" tf:"id,omitempty"`
}

type MetricsParameters struct {

	// (Boolean) When enabled, you can ingest the le dimension, representing explicit histogram buckets.
	// Enable this if you are using OpenTelemetry histograms or Prometheus histogram metrics.
	// When disabled, only your histograms' sum and count metrics will be ingested.
	// When enabled, you can ingest the `le` dimension, representing explicit histogram buckets.\
	// Enable this if you are using OpenTelemetry histograms or Prometheus histogram metrics.\
	// When disabled, only your histograms' sum and count metrics will be ingested.
	// +kubebuilder:validation:Optional
	EnableHistogramBucketIngest *bool `json:"enableHistogramBucketIngest,omitempty" tf:"enable_histogram_bucket_ingest,omitempty"`
}

// MetricsSpec defines the desired state of Metrics
type MetricsSpec struct {
	v2.ManagedResourceSpec `json:",inline"`
	ForProvider            MetricsParameters `json:"forProvider"`
	// THIS IS A BETA FIELD. It will be honored
	// unless the Management Policies feature flag is disabled.
	// InitProvider holds the same fields as ForProvider, with the exception
	// of Identifier and other resource reference fields. The fields that are
	// in InitProvider are merged into ForProvider when the resource is created.
	// The same fields are also added to the terraform ignore_changes hook, to
	// avoid updating them after creation. This is useful for fields that are
	// required on creation, but we do not desire to update them after creation,
	// for example because of an external controller is managing them, like an
	// autoscaler.
	InitProvider MetricsInitParameters `json:"initProvider,omitempty"`
}

// MetricsStatus defines the observed state of Metrics.
type MetricsStatus struct {
	v1.ResourceStatus `json:",inline"`
	AtProvider        MetricsObservation `json:"atProvider,omitempty"`
}

// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:storageversion

// Metrics is the Schema for the Metricss API. The resource
// +kubebuilder:printcolumn:name="SYNCED",type="string",JSONPath=".status.conditions[?(@.type=='Synced')].status"
// +kubebuilder:printcolumn:name="READY",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="EXTERNAL-NAME",type="string",JSONPath=".metadata.annotations.crossplane\\.io/external-name"
// +kubebuilder:printcolumn:name="AGE",type="date",JSONPath=".metadata.creationTimestamp"
// +kubebuilder:resource:scope=Namespaced,categories={crossplane,managed,dynatrace}
type Metrics struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`
	// +kubebuilder:validation:XValidation:rule="!('*' in self.managementPolicies || 'Create' in self.managementPolicies || 'Update' in self.managementPolicies) || has(self.forProvider.enableHistogramBucketIngest) || (has(self.initProvider) && has(self.initProvider.enableHistogramBucketIngest))",message="spec.forProvider.enableHistogramBucketIngest is a required parameter"
	Spec   MetricsSpec   `json:"spec"`
	Status MetricsStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// MetricsList contains a list of Metricss
type MetricsList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []Metrics `json:"items"`
}

// Repository type metadata.
var (
	Metrics_Kind             = "Metrics"
	Metrics_GroupKind        = schema.GroupKind{Group: CRDGroup, Kind: Metrics_Kind}.String()
	Metrics_KindAPIVersion   = Metrics_Kind + "." + CRDGroupVersion.String()
	Metrics_GroupVersionKind = CRDGroupVersion.WithKind(Metrics_Kind)
)

func init() {
	SchemeBuilder.Register(&Metrics{}, &MetricsList{})
}
